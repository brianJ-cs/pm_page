# 詞彙表 / Glossary

同一個字我們常常各指各的（尤其是「block」）。這份表把 UI 上的說法、口語的說法、
程式裡的名字對起來。**程式裡叫什麼**那一欄是可以直接搜尋的。

---

## 一、由大到小：東西的層級

| 詞 | 指的是 | 程式裡叫什麼 |
|---|---|---|
| **檔期** | 一次的 DM 專案，有名字、起迄日、十個階段 | `currentPlan` / `plans[]`，存在 `localStorage['catalogue_plans_v1']` |
| **三方合併** | 送上去之前先跟遠端那一份合：我沒動的用他的、他沒動的用我的、兩邊都動的用我的並講出來 | `mergePlanInto()`、`afterMerge()`、`PlanSync.writeOne()`（CAS） |
| **誰在看哪裡** | 心跳。小地圖上那一塊的角落有個字母＝那個人正在看它；卡片上「設計 B 在看」 | `beat()`、`pullBeats()`、`__presence:<userId>` |
| **輪詢／撞到別人** | 每 5 秒問一次哪幾檔變了。開著的那一檔不換掉，改成掛一條提示；動過手就停止上傳 | `pollRemote()`、`#remoteBar`、`remoteBlocked`／`uploadHeld`、`PlanSync.pullIndex()`／`stampOf()` |
| **卡片小地圖** | 檔期卡下緣那根「小地圖」把手拉開的兩張版。一塊版位一格，底色＝挑好貨的比例。不能點 | `appendCardMap()`、`cardMiniNode()`、`mapOpen` |
| **檔期列表那一行** | 新增檔期／匯入／過往檔期／回收桶／清空資料 —— **整排只有行銷看得到**。設計和 PM 只剩標題和名牌 | `syncListButtons()` |
| **開檔表** | 行銷開一個新檔期／改時程的那張表：名字、開始日、總長、DM 類別、標籤，加一條十個階段的時間軸（旗子拖得動） | `PlanForm.open(plan\|null)`、`#view-plan`；CSS 整包收在 `#view-plan` 底下 |
| **目前階段管理列** | 檔期列表最上面那條（收得起來）：每一檔現在走到哪一階段（**只由日期算**）。點一個階段＝把這台機器的「現在」撥到那一關 | `#statusBar`、`autoStageIndex()`、`jumpToStage()` |
| **撥時鐘 🕒** | 名牌旁邊那顆（devtool，**三個身分、每一頁都有** —— 它在 `#whoBadge` 裡面，跟著 `hostWho()` 走）。打一個日期、或 ±1／±7 天、±3 時、回到現在。改的只有這台機器眼裡的「現在」：不進 plan、不同步。網址 `?now=2026-08-20T10:00` 是同一件事 | `#clockBtn`／`#clockPop`、`now()`／`nowMs()`／`nowOffset`（`pm_fake_now_v1`）、`applyClock()` |
| **帶入** | 開檔表上那顆：照過去某一檔開，連版位切割和品類一起帶。**只複製骨架** —— 貨、便利貼、走到第幾關都不跟過來 | `cloneSkeleton()`、`#impMask`（在 `PlanForm` 裡面） |
| **草稿** | 行銷開了、還在切版、還沒提交的檔期。只有行銷看得到 | `plan.submitted === false`（**沒有這個欄位＝已提交**）；`isDraft()`、`visiblePlans()`、`renderSubmitBtn()` |
| **正面 / 背面** | 一張 DM 的兩面 | `plan.layout.front` / `plan.layout.back` |
| **版位** | 版面切出來的一塊區域，也就是「一個 section」。小地圖上一個粉紅方塊＝一個版位 | 樹葉節點 `block`；`collectBlocks()` 撈出來的東西 |
| **品類** | 版位裡的商品分類，例如「小家電」。一個版位可以有好幾個品類 | `block.groups[] = {name, count}` |
| **支** | 商品數量單位。「9 支」＝九個商品格 | `count` / `totalCount(block)` |
| **格 / 卡片** | 版位裡的一個商品位置。落版上是白色方塊，拼板上是一塊畫布 | `sheet.cells[] = {id, text, gi, notes[], board}` |
| **列** | 格子排成的一橫排 | `sheet.rows[]`（每一列幾格）；DOM 上是 `.row` |
| **刊頭** | 版位裡放主視覺的那一塊，可以在上、下、左、右 | `sheet.masthead`、`sheet.mastheadH/W`、`sheet.mhSide`、`sheet.mastheadRow` |

> ⚠️ **「block」是最容易誤會的字。** 中文講「版位」時我們指 section；
> 但講「the blocks」時可能是指**格子**，也可能是指畫布裡的**零件**（見下）。
> 說的時候盡量用「版位 / 格 / 零件」，不要用 block。

---

## 二、三個分頁（畫面模式）

| 詞 | 在做什麼 | 程式裡叫什麼 |
|---|---|---|
| **總體** | 一次看正反兩面所有版位 | `activeTab === 'total'` |
| **落版** | 排列：切幾列、每列幾格（旁邊的 ＋／−）、刊頭放哪、拿 搬移 互換格子。格子上只寫「未填寫／有填寫 ✓」，品名不在這一頁 | `activeTab === 'sheet'` |
| **拼板** | 每一格裡實際拼商品：挑貨、擺圖、標便利貼 | `activeTab === 'board'` |

---

## 三、拼板裡的東西

| 詞 | 指的是 | 程式裡叫什麼 |
|---|---|---|
| **畫布** | 一格裡跑的那個獨立網頁，商品版面本人（不是縮圖） | `2cell-product-pick.html?ui=stage`，`.ccanvas` iframe |
| **面板** | 點一格之後跳出來的挑貨／圖層面板 | `#boardStage` + `?ui=panel` |
| **零件** | 畫布裡可以自由搬的每一塊：品牌、品名、機型、商品圖、規格、SKU、價格 | `BLOCKS[] = {kind:'brand'\|'name'\|'model'\|'img'\|'spec'\|'sku'\|'price', x, y}` |
| **挑貨** | 勾選這一格要放哪些商品 | `picked` / `cell.board.state.picked` |
| **單塊大小** | 某一塊零件自己被放大縮小過幾倍（不影響同類的其他塊） | 圖是 `b.w`／價格是 `b.fsFix`／其他是 `b.sc`（CSS 那邊 `--bs`） |
| **刪掉的零件** | 從版上拿掉、但還找得回來的零件。格子右上角那顆 ＋ 列的就是它 | `cell.off = {uid: 名字}`；`b.uid` 是重建也不會變的身分 |
| **安全邊界** | 零件不可以進入的邊界，四邊各 8（排版座標單位，不是螢幕 px） | `MARGIN`（`2cell-product-pick.html`）。**只有這一個名字** —— 以前是左右／上下／價格例外三個常數，四邊一樣寬只是剛好填了同一個數字 |
| **排版座標系** | 畫布內部用的座標，寬固定 540、高照格子形狀算 | `CW`、`CH` |

---

## 四、留言與交接

| 詞 | 指的是 | 程式裡叫什麼 |
|---|---|---|
| **便利貼 / 貼紙** | 貼在格子上的一則問題，可以拖、可以回覆、可以標成處理完 | `cell.notes[] = {key, x, y, who, time, done, replies}` |
| **貼紙種類** | 補貼紙 / 補贈品 / 一般問題 | `NOTE_KINDS` |
| **意見欄** | 右邊那一欄，一則便利貼一張卡 | `#boardRail`、`.icard` |
| **關（現在走到第幾關）** | 提品 → 製作 → 一校 → 一改 → 二校 → 二改 → 蓋章 → 蓋章修改 → 審稿 → 出稿。**整本 DM 一起走，而且完全由時間決定**（檔期表的 `start`＋`durs` 排出來的日期）—— 誰都推不動它 | `STAGES`、`STAGE_SIDE`、`planStageIndex()`、`stageNow()` |
| **球在誰手上** | 這一關是 PM 的還是設計的。**不存在資料裡**，是這一關查出來的 | `planSide(plan)`、`myTurn()`（橫幅上那句「輪到你」） |
| ~~交棒／蓋章／蓋章模式／送給 PM~~ | **沒有這回事了**（2026-08）。以前是一格一顆棒子（`cell.st` 0..7），兩個身分互相蓋章往前推。現在整本照時間走，所以 `cell.st`、`#stampBtn`、`#stampBar`、`#confirmBtn` 整套拿掉。舊檔期資料裡還留著 `cell.st`／`cell.at`，沒有人讀它 | — |
| **版位狀態** | 每個版位左上角那行小字，講的是**這一塊的貨挑齊了沒**：還沒排版／還沒挑貨／挑好 n/m 格／都挑好了。跟底色（灰／白／綠）講同一件事，只是顏色講不出「一半」 | `blockState()`、`cellPicked()` |
| ~~負責 PM／指派模式~~ | **沒有這回事了**（2026-08）。三位 PM 對整本 DM 一視同仁 —— 每個人都看得到、點得進去、改得動。`cell.pm`、指派模式、格子上那個「・甲」、小地圖藍點、PM 左欄的「我的品類」整套拿掉。舊檔期資料裡還留著 `cell.pm`，沒有人讀它 | — |
| **身分** | 在**登入頁**選一個人決定，進去之後不能撥（身分列上那三顆和換 PM 的下拉都拿掉了），要換就右上角「登出」。行銷看得到三個分頁、改得動 落版，拼板 只能看（工具列照樣在，擋的是動作：不開格子、不貼貼紙，畫布收到的身分是 `'pm'`） | `activeMode` ＝ `currentUser.role`；`login()`、`isMkt()`、`canvasRole()`、`syncTools()` |
| **使用者名單** | 設計／PM／行銷各三位（設計 A／B／C…），登入頁上可以加人。**id 絕不重用**：便利貼記的是 `whoId`，換了 id 舊留言就變成別人的 | `USERS`（`board_users_v1`）、`usersOf()`、`currentUser` |

> ⚠️ **交接不是一個動作，是一個日期。** 走到第幾關由 `STAGES` 那條時間軸算出來
> （`autoStageIndex()`：第一個 18:00 還沒過的那一關），跑完了就停在出稿。
> 沒有人推得動它 —— 要提前或延後就去改檔期的時程（行銷的「改時程」）。
>
> **`cell.st`（一格一顆棒子）和 `sheet.confirmed`（列排列鎖定）都整條拿掉了。**
> 舊檔期資料上可能還留著那兩個欄位，沒有人讀它。
>
> **不壓暗、不擋。** 整本一起走，所以「不是你的回合」會是整張版一起暗 ——
> 那沒有指出任何東西，只會被當成程式壞了。現在是哪一關寫在橫幅上一行字裡
> （`updateProgress()`：`現在：一改（設計・輪到你）　倒數 2 天・挑好 88/352 格`）。
> 便利貼任何時候都貼得了：它是對話，不是版面的一部分。
>
> **要看別的關長什麼樣就撥時鐘**（管理列點一個階段，或網址 `?now=2026-08-20T10:00`）——
> 那是自己這台機器的畫面，不進資料、不同步。

---

## 五、工具列（落版）

三個工具講的都是「**點下去**會發生什麼」。滾輪縮放、拖空白處移動畫面是**每個工具都有**的。

| 圖示 | 名稱 | 快捷鍵 | 點下去 | 程式裡 |
|---|---|---|---|---|
| ⇄ | 搬移 | V | 交換／插入格子 | `'move'` |
| ✥ | 選零件 | **S** | 在格子裡選、拖品名／價格／商品圖 | `'edit'` |
| ✋ | 拖畫面 | **D** | 拖哪裡都是移動畫面，包括格子上 | `'pan'` |
| ⊞ | 顯示格線 | G | 開關（不是工具）：一層看得到的格線，**不吸** | `gridLines` |
| ⧉ | 整格複製 | — | 挑一格，Ctrl+C / Ctrl+V 整格複製 | `'copy'` |
| ⌕ | 看版 | — | 什麼都不會動到，只有縮放和移動畫面 | `'zoom'` |

> S 和 D 挨在一起是故意的（跟 Ctrl+C／Ctrl+V 同一個道理）：選東西和拖畫面一直在
> 交替用，手不該為了換工具跑到鍵盤另一頭。**S＝select、D＝drag**。

> **每一列右邊的 ＋／−**（`.rowbtns`、`changeRowCells()`）＝這一列加一格／少一格。
> 三個地方都有：設計的 落版、設計的 拼板、PM 的 拼板（版面是同一支 `renderGrid`）。
> ＋ 加的那一格算在這一列最後一格的品類底下（`groups[gi].count` 跟著加），
> − 拿掉這一列最後一格；品類被減到 0 就整條拿掉。動過就把每列幾格釘進 `sheet.rows`
> ——不釘的話下一次重畫會照 `splitEven` 重分，剛加的那一格就跑到別列去了。
> 按鈕是**絕對定位貼在版面外面**的：版位的大小和比例不准被工具動。

> 以前還有一顆 ✎ 輸入（在格子裡打品名）。落版的格子改成只顯示狀態之後就拿掉了 ——
> 品名是拼板挑完貨才有的東西。`cell.text` 這個欄位還在，舊檔期打過的字沒有刪，
> 拼板的「顯示落版文字」還看得到。

---

## 六、講尺寸的時候

這幾個字我們吵最久，先講定：

| 說法 | 意思 |
|---|---|
| **固定大小** | 不管版面放大縮小，它在螢幕上永遠同一個 px（例：便利貼、紅色數字、版位標題那一行） |
| **跟著縮放**（＝之前講的 "flex"） | 版面放大它就一起放大，因為它是版面的一部分（例：格子裡的品名） |
| **只縮不放** | 版面縮小時跟著縮，放大時停在原本大小（目前只有紅色數字這樣） |
| **縮放** | 只是把畫面拉近拉遠來看，**不會改變印出來的東西**。三頁都是滾輪縮放（Ctrl +／−／0 同一組）；總體 的倍率是乘在「符合視窗」上面的，1 ＝整本剛好塞滿 | 落版／拼板 `sheetZoom`＋CSS 變數 `--z`；總體 `totalZoom`／`totalZoomTo()`／`totalZoomFit()` |

---

## 七、檔期的十個階段

`STAGES`：提品 → 製作 → 一校 → 一改 → 二校 → 二改 → 蓋章 → 蓋章修改 → 審稿 → 出稿

每一關是誰的事寫在 `STAGE_SIDE`（PM／設計交替，審稿和出稿都算 PM）。
**這一份就是「球在誰手上」的全部答案** —— 版面上不再有第二個地方存這件事。

---

## 八、檔案

| 檔案 | 是什麼 |
|---|---|
| `design_and_PM.html` | 主程式（編輯器）：檔期列表、總體、落版、拼板、意見欄 |
| `2cell-product-pick.html` | 商品版面本人。被主程式用兩種方式嵌進去：`?ui=stage`（畫布）、`?ui=panel`（面板） |
| `supabase-sync.js` | 同步層。零相依，只用 `fetch` 打 PostgREST。掛在 `window.PlanSync` |
| `config.js` | Supabase 網址和 anon key。**沒有 build，所以設定是靠這一支注入的** |
| `public/index.html` | build 出來的就是 `design_and_PM.html`。`/editor` 靠 `_redirects` 指回 `/`（舊書籤） |
| `build-single.mjs` | 把要公開的檔案複製進 `public/`（Netlify 的 build）。`--bundle` 才會合成單檔 |
| `dev.mjs` | 本機預覽。`PM_NO_SYNC=1 node dev.mjs` ＝不連後端 |
| `public/` | build 產出來的，不是正本。正本是根目錄那幾支 |
| `clutter/` | 舊版、備份、原型（`cell_clipboard_demo.html`、`grid-reorder.html` 都收在這裡）。整個資料夾不進版控 |

---

## 九、資料存在哪裡

**兩層：`localStorage` 是本機快取，Supabase 是共用的那一份。**
開機先用 `localStorage` 秒開，再拉一次遠端蓋上去；每次存檔照舊寫 `localStorage`，
另外 debounce 600ms 上傳。後端不在（`config.js` 還是佔位符、或檔案不在）時
`PlanSync.ok()` 回 false，整支走純 `localStorage`，跟沒接後端一模一樣。

> ⚠️ **所以「用瀏覽器直接開這幾支」＝在正式資料上按按鈕。**
> 測試一律走 `PM_NO_SYNC=1 node dev.mjs`，或 `driver.mjs`／`scenario.mjs`（它們自己會擋）。
> 細節見 `.claude/skills/pm-page-traps`。

### 本機（localStorage）

| key | 內容 |
|---|---|
| `catalogue_plans_v1` | 所有檔期（版位、格子、便利貼、拼好的畫布，全都在裡面） |
| `catalogue_trash_v1` | 回收桶（刪掉的檔期）。**只在這台機器上** —— 共用資料庫那一列刪的時候就拿掉了 |
| `board_users_v1` | 使用者名單（設計／PM／行銷各三位）。`board_pms_v1` 是它的前身，開機時搬過來、id 原封不動 |
| `board_user_at_v1` | 上次登入的是誰。清掉它＝登出 |
| `board_pms_v1` | 舊的 PM 名單（只在第一次開機被讀一次，之後沒人寫它） |
| `board_pm_owner_v1`、`board_pm_active_v1` | 舊的，沒有人讀了。名字留著只是為了「清空資料」清得掉它們 |

### 共用（Supabase）

| 放哪 | 內容 |
|---|---|
| `plans` 表 | 一列一個檔期，整包 `data jsonb`。同一個 id **後存的贏** |
| `catalogue` 表 | 商品目錄，全公司一份，單一列（`id = 'default'`） |
| Storage `product-images` | 商品圖、品牌圖、刊頭圖。**圖不進 plan，只存網址** —— base64 會把 `localStorage` 撐爆，而且每次存檔重傳一遍 |

清空：檔期列表右上角的 **清空資料**（行銷才看得到；只清這台瀏覽器，共用資料庫上的重整就會拉回來）。
想要範例檔期就在網址加 `?seed=1`。
