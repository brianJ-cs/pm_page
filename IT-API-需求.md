# 給資訊部：DM 落版／拼板系統需要的 API

這支系統目前**所有的主檔資料都是假的** —— 商品是人工貼上去的、使用者是寫死的九個人、
DM 類別和尺寸寫在程式裡。要正式上線，下面這幾份資料需要從公司系統接過來。

每一項都註明「現在怎麼來的」和「在程式裡的哪裡」，方便對照。

---

## 一、優先順序

| 順位 | 要什麼 | 為什麼是這個順位 |
|---|---|---|
| **P0** | 商品主檔 | 沒有它整支系統沒有內容可以排。目前靠人工從 Excel 貼進來 |
| **P0** | 人員名單＋登入 | 現在是假登入、沒有密碼，九個人寫死在程式裡 |
| **P1** | 商品圖／品牌 LOGO | 現在要人一張一張上傳 |
| **P2** | 檔期主檔 | 現在由行銷在系統內自己開，可接受；但若公司已有排期系統就該接 |
| **P2** | 品類代碼表 | 現在是自由輸入的文字，會有錯字 |

---

## 二、商品主檔（P0，最重要）

### 現在怎麼來的
人工把 Excel 貼進「後台」的貼上框，程式用空白／Tab 去猜欄位
（`2cell-product-pick.html:2540` 附近）。猜錯就是排錯版。

### 需要的欄位

| 欄位 | 型別 | 必要 | 用在哪 | 備註 |
|---|---|---|---|---|
| `sku` | 字串 | ✅ | 商品的唯一身分 | **合併資料時是用它比對的**，必須唯一且穩定 |
| `name` | 字串 | ✅ | 品名，版面上最大的那一行 | 同一個 `name` 的商品會被排在同一格 |
| `spec` | 字串 | ✅ | 規格（如 `2TB`） | |
| `color` | 字串 | ⬜ | 顏色 | 可空 |
| `price` | 數字 | ✅ | 售價 | 純數字，不要千分位、不要 `$` |
| `model` | 字串 | ⬜ | 型號（如 `A1654H21`） | 目前是「品名 → 型號」的對照表 |
| `brand` | 字串 | ⬜ | 品牌（如 `Apple`） | 目前是「品名 → 品牌」的對照表 |

### ⚠️ 要請資訊部確認的三件事

1. **`model` 和 `brand` 請掛在 SKU 上，不要掛在品名上。**
   現在程式是用**品名字串**去查型號和品牌（`2cell-product-pick.html:481`、`:497`）。
   品名只要差一個空白就查不到。給的資料若能一列一個 SKU、型號品牌各自帶著，
   我們這邊就能改成用 SKU 查。

2. **價格只有一個欄位，夠嗎？**
   目前只存一個 `price`。DM 上常見「原價／特價」兩個數字 —— 如果需要，
   請一併提供 `list_price`（原價）和 `sale_price`（特價），我們再決定版面怎麼呈現。

3. **一次大概幾筆？** 需要知道量級來決定要不要分頁。
   目前程式是「整包拿回來放在記憶體裡」。

### 希望的介面

```
GET /api/products?updated_since=<ISO8601>
→ [{ sku, name, spec, color, price, model, brand, image_url, brand_logo_url }, ...]
```

`updated_since` 很重要：這支系統會定時去問有沒有更新，能只拿異動的就不必每次整包搬。
**唯讀即可**，我們不會寫回商品主檔。

---

## 三、人員與登入（P0）

### 現在怎麼來的
程式裡寫死九個人（設計 A／B／C、PM A／B／C、行銷 A／B／C），
**點名字就登入，沒有密碼**（`design_and_PM.html:8296` 附近）。

### 需要的欄位

| 欄位 | 型別 | 必要 | 用在哪 |
|---|---|---|---|
| `id` | 字串 | ✅ | 系統內部認人用 |
| `name` | 字串 | ✅ | 顯示在名牌和便利貼上 |
| `email` | 字串 | ✅ | 通知、以及跟公司帳號對應 |
| `role` | 列舉 | ✅ | 只有三種：`design`／`pm`／`mkt`（行銷） |
| `department` | 字串 | ⬜ | 部門 |
| `title` | 字串 | ⬜ | 職稱 |

### ⚠️ 要請資訊部確認的三件事

1. **`id` 一旦給了就絕對不能變、不能重用。**
   系統裡每一則便利貼都記著「這是誰貼的」，記的就是這個 id。
   id 被回收給另一個人，舊的留言就會變成別人說的話。

2. **`role` 怎麼對應？**
   公司的組織資料裡不會有「design／pm／mkt」這三種。需要一個對應規則
   （用部門？用職稱？還是給一份名單？）。**這個系統的整個流程都建立在這三種身分上**，
   對錯了就是權限錯。

3. **登入要怎麼做？** 最理想是接公司 SSO（拿得到 email 和上面那些欄位）。
   若短期做不到，退而求其次是給一份人員清單，我們自己做一個簡單的登入。

### 希望的介面

```
GET /api/users            → 名單（上面那些欄位）
GET /api/me               → 登入後的自己（SSO 情境）
```

---

## 四、商品圖與品牌 LOGO（P1）

### 現在怎麼來的
人工一張一張上傳，存在我們自己的雲端空間。

### 需要的
- 每個 SKU（或每個 品名＋規格＋顏色 組合）對應的**商品圖網址**
- 每個品牌的 **LOGO 網址**

### ⚠️ 技術上必須確認
1. **圖片網址要能從瀏覽器直接讀取**（公開網址，或帶得了我們的權杖）。
2. **CORS 要放行我們的網域** —— 這一條沒開的話圖片會讀不出來，
   而且是那種「網址貼到瀏覽器打得開、程式裡就是不行」的狀況。
3. 圖片若會換（改包裝、換色），網址請跟著換或加版本號，否則會一直看到舊圖。

---

## 五、檔期主檔（P2）

目前由行銷在這支系統裡自己開，**這樣也可以接受**。
但如果公司已經有排檔期的系統，就該接過來，不要兩邊各記一份。

一個檔期目前存這些（`design_and_PM.html:9413`）：

| 欄位 | 說明 |
|---|---|
| `name` | 檔期名稱，如「7月中檔・全省對開大DM」 |
| `type` | DM 類別（決定版面幾公分，見下一節） |
| `tags` | 標籤，如 `大DM`、`促銷`、`母親節` |
| `start` | 起始日 |
| `durs` | 十個階段各幾個工作天 |

十個階段是：提品 → 製作 → 一校 → 一改 → 二校 → 二改 → 蓋章 → 蓋章修改 → 審稿 → 出稿
（`design_and_PM.html:9391`）。**請確認這十關和公司實際流程一致。**

---

## 六、DM 類別與尺寸（P2）

現在寫死四種（`design_and_PM.html:2799`）：

| 類別 | 尺寸 |
|---|---|
| 全省對開大DM | 78.7 × 54.5 cm |
| 網路素材 | 42.0 × 29.7 cm |
| 單店制作物 | 42.0 × 29.7 cm |
| A3 | 42.0 × 29.7 cm |

**請確認這份清單和實際尺寸。** 其中三種是先照 A3 填的，不一定對。
尺寸錯了整張版的比例就錯，設計排出來的東西印出來會不對。

---

## 七、品類代碼表（P2）

版面切好之後，行銷要填「這一塊放哪些品類、各幾支」。
現在是**自由輸入的文字**，所以「行動電源」和「行動電源 」會被當成兩種。

希望拿到一份品類清單（代碼＋名稱），做成下拉選單。

---

## 八、非資料面的需求

這幾項不是資料，但沒有的話接不起來：

1. **CORS**：請把我們的網域加進允許清單。
2. **認證方式**：API 用什麼驗證？（API key／OAuth／內網 IP 限制）
   注意這是一支**在瀏覽器裡跑的**程式，放在前端的金鑰等於公開 ——
   若不能公開，就需要一個中介服務。
3. **呼叫頻率上限**：這支系統為了讓多人共同編輯時能即時看到對方的改動，
   會**每 1.2 秒**去問一次有沒有更新（只在有人同時在線時）。
   如果商品／人員 API 也要這樣問，請先告訴我們上限，我們會調整頻率或改成快取。
4. **測試環境**：希望有一組測試資料可以打，不要一開始就接正式資料。

---

## 九、我們這邊不需要對方提供的

避免誤會，這幾件事是這支系統自己管的，不必麻煩資訊部：

- 版面怎麼切、每一塊放什麼、商品擺在哪個位置
- 便利貼、每一格走到第幾關、誰把工作交給誰
- 檔期的歷史紀錄與復原

這些都存在系統自己的資料庫裡。
