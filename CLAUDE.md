# pm_page

DM 落版／拼板 編輯器。**單檔 HTML，沒有 npm、沒有相依套件、沒有打包** —— 用瀏覽器直接開就是完整的程式。請維持這件事。（`build-single.mjs` 叫 build，但它只是把檔案複製進 `public/`；同步層也是手寫 `fetch`，不引 CDN 函式庫。）

## 最高原則：長得像 `rule.png`

**`rule.png` 就是規範。** 這支程式排出來的東西 —— **不只是最後的成品，第一版草稿就要** ——
要盡可能接近 `rule.png` 那一張真正印出去的 DM。`ref.png` 是它商品格那一區的放大，
談格子裡面怎麼擺的時候看那一張。

- 這條**壓過所有其他規矩**。任何說法（舊的驗收標準、舊的參考檔、這份文件底下任何一段、
  技能裡寫的標準）跟「長得像 `rule.png`」衝突時，**照這一條**，並且把那段舊的改掉，
  不要留著兩套標準各說各話。
- **`2cellOriginal.html` 不再是規範**，它降級成一把尺（`2cell-parity` 那支還能用來量
  「改動前後差多少」，但它量出來的差**不是**驗收結果）。
- **預設就要對。** 「使用者自己搬一搬就會像了」不算 —— 挑完貨、什麼都還沒動的那一版，
  就該長得像 `rule.png`。
- 驗收的方法是**看**：`run-pm-page` 截圖出來跟 `rule.png` 並排比，不是只看數字。

| 檔案 | 做什麼 |
|---|---|
| `design_and_PM.html` | **整支程式**：登入、檔期列表、開檔表＋時間軸、總體／落版／拼板、意見欄。三個身分都從這裡進。所有程式碼在**同一個** `<script>` 裡 |
| `2cell-product-pick.html` | 商品版面本人。被主程式嵌兩次：`?ui=stage`（格子裡的畫布）、`?ui=panel`（挑貨面板），靠 `postMessage` 溝通 |
| `product.html` | **商品目錄**（全公司一份）。匯入 CSV、分類別、改欄位。主程式裡是浮層，也有自己的網址 `/products` |
| `logo_page/` | **品牌 Logo 庫**，本來是獨立的一站，整包搬進來當浮層（`/logos`）。**只有設計看得到** |
| `sticker_editor.html` | **貼紙庫**：促銷、規格、贈品那些可以重複貼的小圖，一張貼紙是一疊圖層。主程式裡是浮層（`?embed=1`），也有自己的網址 `/stickers`。**只存這台瀏覽器**、**只有設計看得到** |
| `sticker-render.js` | **一張貼紙怎麼畫出來**。貼紙庫和格子裡的畫布共用同一份 —— 抄兩份的話貼紙庫改一點版面，版上就跟著錯而且看不出來 |
| `supabase-sync.js` ＋ `config.js` | 共用資料層。零相依，只用 `fetch` 打 PostgREST；沒有 build，設定就靠 `config.js` 注入 |
| `_redirects` | `/` ＝主程式；`/editor` 指回 `/`（行銷那一支併進來之前，編輯器住在那裡）；`/products`、`/logos` ＝那兩個庫的直達網址 |
| `schema.sql` | `plans` 和 `catalogue` 的建表 SQL。**事後補的**（那兩張當初是在後台用點的），對著在跑的專案跑是安全的 —— 全部 `if not exists`，policy 只在還沒有的時候才建 |
| `GLOSSARY.md` | **動手前先看**。「block」在這個專案有三個意思（版位／格／零件），吵過很多次 |

## 改完一定要跑起來看

語法檢查**擋不住這裡最常見的錯**。整支程式是一個 `<script>`：在頂層呼叫一個會讀到下面才宣告的 `let` 的函式 → `ReferenceError` → 整支程式停在那一行 → 開起來整頁是死的，而「語法沒問題」完全看不出來。踩過不只一次（最近一次是 `}` 插錯一行，`node --check` 照樣過）。

⚠️ **兩支技能自己叫起來，不必等人開口：**

- **`run-pm-page`** ＝跑起來看。怎麼跑、**這次要驗到哪一級**（只開起來 3.7 秒／真的點一下 5.6 秒／動到格子裡的零件 17 秒／連兩把尺一起跑，分鐘級）、看到什麼才算過、以及點不到東西時的那幾個坑（快捷鍵被輸入框搶走、工具列捲出畫面、便利貼標記坐在格子中央）。**每次改動都挑一級，不是每次都跑最貴的那一套。**
- **`edit-fast`** ＝改東西的節奏。一批改完再驗；還有怎麼動檔案（一律寫一支 `.mjs` 用錨點改，**不要在 shell 裡寫 JS**；這個 repo CRLF 和 LF 混著；`python` 是假殼；`| cat -n` 會卡住）。2026-09 那一輪點過：一個四檔案的改動，**約十次工具呼叫是在跟引號打架**，不是在改東西。

## 顏色

- **新顏色一律在 `:root` 取個名字**，不要往下面塞 hex。名字照「做什麼用」取（`--surface`、`--ink-dim`、`--accent-tint`、`--danger-ink`），不照「是什麼顏色」。
- **藍色＝「你正在點的東西」**：分頁、選取、放下去的位置。東西本身不准用藍。
- **便利貼的顏色＝誰貼的**（設計天藍 `--sticky-design`／PM 黃 `--sticky-pm`，登入頁那兩顆點讀同一個名字），
  **圖示＝哪一種**，角落那個字＝哪一位（「設計乙」→ 乙）。判身分看 `whoId` 的前綴（`design:d1`），
  **不可以比 `=== 'design'`** —— 簽名變成「設計乙」之後那樣寫會把設計的貼紙判成 PM 的，只會被當成「顏色怪怪的」。
- **桌面偏的那一點色＝你是誰**（2026-08 加）。三個身分沿用便利貼那套名字
  （`--sticky-design` 天藍／`--sticky-pm` 黃／`--sticky-mkt` 薄荷綠，行銷那個只有 UI 在用）。
  `applyMode()` 把 role 寫進 `body.dataset.role`，CSS 自己去挑 `--role`。
  染的只有三處，**全部在桌面那一層**：**最上面那條列整條就是身分色**
  （`.topbar`／`#editorChrome > .bar`，2026-08 從「上緣一條 3px」改成滿版 ——
  線太細，一個人同時開兩個視窗時還是要湊近看）、body／`#boardRail` 的底混 7%、
  名牌 `#whoBadge` 左邊一條 3px（那條在開檔表的標題列上還有用，那條列不是身分色）。
  **藍和紙一點都不動** —— 藍是「你正在點的東西」，
  紙是要印出去的，兩者都不該因為誰在看而變色。7% 是下限：再多就開始像主題色，
  而它只是一個記號（一個人常常同時開兩個視窗，自己用兩個身分對照著看）。
  ⚠️ `.bar` 要指名 `#editorChrome > .bar`：檔期卡上的進度條也叫 `.bar`。
- **版位的底色＝狀態**：灰＝還沒提明細、白＝設計手上、綠＝整塊都在 PM。
  小地圖和總體現在**共用同一套**（鎖定那條特例跟著鎖定一起拿掉了）。
- **格子的底色＝這一格印什麼顏色**（`cell.bg`，2026-08 改成一格一個）。它跟刊頭圖同一層：
  是**印出來的一部分**（真正的 DM 上一個品類就是一條色帶），不是看版的人的偏好 ——
  存進檔期、跟著同步、**只有設計上得了**。
  **工具在 拼板**（`#toolRail` 的 🎨，快捷鍵 C）：**點一格上色、拉一個框整批上色、
  Alt＝清掉底色**。為什麼是 拼板 不是 落版：底色是配給**貨**的，而只有 拼板 上看得到
  那一格真正長什麼樣（品名、商品圖、那幾個紅色的價格）—— 落版 的格子還是空的，
  在那裡挑顏色等於憑空猜。
  取色盤裡那顆 💧 是**取色**（快捷鍵 I）：點一格＝把那一格的底色拿起來，然後自己換回 上色 ——
  取色是上色途中的一步（把某一格的顏色借去塗別幾格），留在取色上的話下一下點出去
  什麼都不會發生。取的是 `cell.bg` 那個色碼，不是螢幕上的像素：格子上蓋著壓暗、hover、
  半透明的畫布，量出來一定差一點，而差一點在印刷上就是另一個顏色。那一格沒上過色
  就講一句（沒有欄位可取），點在版上的空地不擋 —— 那一下還是平移。
  它站在盤子裡、不站在工具列上：拿起來之後手上換的是**顏色**，不是「點下去會發生什麼」，
  而那一盤講的正是手上拿著什麼顏色（工具列上亮的還是 🎨，不然整條會一顆都不亮）。
  **游標寫著手上是哪一件事**（`--cursor-bucket`／`--cursor-dropper`，做法照 總體 切分那兩顆）：
  油漆桶＝倒下去、滴管＝吸上來 —— 兩件事方向相反，共用一個十字游標分不出來。
  取色盤（`#paintPop`）是那顆工具的一部分，收起來、換分頁、拿別的工具都跟著收；
  **它站在工具列左邊**（`placePaintPop`）：右邊就是版面本人，216px 壓上去蓋住的
  正是要看著配色的那幾格。左邊塞不下（左欄收起來）才退回右邊。
  裡面是色環（色相繞一圈、彩度往中心、明度另一條）＋ hex 欄位 ＋ **色盤**
  （`plan.palette`，自己存的）＋ **最常用**（`plan.bgUse`，這一檔上過幾格）。
  PM 和行銷看得到顏色但按不動，那一段的字換成「底色由設計決定」，在版上按下去也講同一句。
  ⚠️ **工具列（`#toolRail`）是 落版 和 拼板 共用的同一顆 DOM**（`hostSheet()` 搬來搬去），
  所以那一顆在 落版 要整個藏起來、切走時要把刷子放下 —— 兩件事都掛在 `syncPaintPop()`
  和 `setTab()` 上。不藏的後果是 落版 多一顆按了沒反應的鈕，不放下的後果是手上拿著
  一個看不到的工具（點格子沒反應，而畫面上沒有任何東西解釋為什麼）。
  ⚠️ **hover 不用藍色蓋掉底色** —— 這一下改的就是底色，點完手停在原地卻看到一塊藍，
  只會以為沒上到（藍框照留）。（「不是你的回合就壓暗」那一條沒有了，見下面那一段：
  上色時被壓到 45% 的顏色不是那個顏色，本來就得例外；現在整張版永遠是亮的。）
  （以前是整本 DM 一個「紙色」`plan.bg`，收在 總體 的工具列裡。那個欄位沒有了 ——
   舊檔期第一次開起來會把它塗到每一格上並當場存檔，見 `migratePaperToCells`。）
  ⚠️ 顏色寫成每一顆 `.cell` 上的 `--cbg`（inline style，`setCellBgEl`），但另外有一支
  `paintCellBgs()` 從資料重刷一遍，掛在 `rebuildAll()` 和合併那幾個出口上 ——
  Ctrl+Z 退掉的可能就是一次上色，而那一趟重建多半走「只改大小」的快路
  （`sheetSig` 沒變 → 卡片還是原來那幾顆，`makeCard` 不會再跑），沒人刷就是
  「資料退回去了、螢幕上還是新顏色」。
  ⚠️ **平移那條要放行**（`boardOv` 的 `pointerdown`）。它本來只擋
  「在格子上按下去」的，而框選常常從格子與格子之間的縫、或版面邊上起手 —— 不擋的話
  畫面跟著手走、格子在框子底下移動，放手時量到的是移動後的位置，**上到色的是完全
  另外幾格**（而且不報錯）。版外那一圈空地不擋，不然拿著刷子就沒有平移的路了。
  ⚠️ 手勢掛在 `boardEl`（`#board`）不是 `boardOv`：`#sheetPane` 在 拼板 是被搬進
  `#boardOverview` 裡面的，格子還是 `#board` 的小孩 —— 而 `boardOv` 上已經坐著平移，
  擠在同一顆節點上只會變成兩個手勢互相搶。
  ⚠️ 點一格不會順手把那一格的面板開起來：`boardOv` 的 click 那條有 `activeTool !== 'edit'`
  就 return，本來就擋住了。
  ⚠️ **自己上過色的格子不吃 `.cell.g2` 那一階**：那一階是「還沒有人指定顏色時讓兩組
  分得出來」的暫代品，再壓一層灰上去只會變成一個他沒選過的顏色。做法是兩個變數：
  `background:var(--cbg,var(--cbase))`，g2 只換 `--cbase`。分成兩個是因為有好幾條規則
  （g2、拿著刷子時的 hover）要在不知道色值的情況下重講一次這個式子。
  ⚠️ **深色底把字翻白**（`on-dark`，感知亮度 <0.58）。落版 的未填寫／有填寫那兩個字
  要指名 `#view-sheet .cell.on-dark::before` —— 原本那兩條帶 id，權重比
  `.cell.on-dark::before` 高，寫在別處會完全沒有作用。
  ⚠️ 清除≠塗白：清除是 `delete cell.bg`（＝從來沒上過色），所以 g2 那一階回得來、
  合併時也不會為了一個等於預設值的欄位報衝突。
  ⚠️ 格子裡面那張紙是另一份 document：`postMessage({paper})`，而且 `d.ready` 那一包就要帶
  （送的是**那一格自己的**顏色，`target` 就是那一格），晚一步送會先閃一次白。
  **stage 模式的畫布本來就是透明的**，所以格子的顏色是主程式那邊的 `.cell` 在畫；
  畫布自己的 `--paper` 真正有差的是挑貨面板（`ui=panel`，它畫的就是那一格）。
  ⚠️ 列印時 `.cell` 的底色是唯一不洗掉的一項（`var(--cbg,transparent)`）：那是設計特地
  上的色，會跟著印出去。g2 那一階不印，所以 fallback 是 `transparent` 不是白。
  ⚠️ 版位的底色（`.window-node`，灰／白／綠＝狀態）**不跟著變**：一塊版位裡可能有
  五種顏色，畫一個平均值只會是一個誰都沒選過的顏色。
  ⚠️ `palette`／`bgUse` 不進 `META_KEYS`：它們是**只會加東西**的東西，逐欄比的話
  兩個人各存一個顏色就會報一個假衝突然後掉一顆。合併走 `mergePaintInto()` ——
  palette 取聯集（只有我拿掉的才算拿掉，照便利貼那條），bgUse 合的是兩邊各自的增量。

- **桌面是一張淡的灰藍**（`--chrome:#ccd5e1`，2026-08 從深藍 `#232a37` 換掉），那一段
  （`操作區＝一張淡的灰藍桌面`）放在 stylesheet **最後面**，靠來源順序覆蓋。
  桌面只比紙（`--bg:#f2f4f8`）深一階：分得出「桌面／紙」就夠了。
  ⚠️ **桌面上「墊高一階」用 `--chrome-fill`**（一層薄墨），不要再寫 `rgba(255,255,255,…)`
  —— 那是深底才成立的，白疊在淺灰藍上完全看不出來。
  ⚠️ 深色時代留下來的那些「自己寫 `color`」還留著（`#blockHead`、`.plan-card`、落版的字）：
  現在兩邊都是深字，看起來像可以省掉，不要省 —— 桌面色是會再被換掉的那一層。

## 大小

| 說法 | 意思 |
|---|---|
| 固定大小 | 螢幕上永遠同一個 px：便利貼以外的標記、版位標題 |
| 跟著縮放 | 乘 `var(--z)`：格子裡的品名、便利貼（它指著版面上某一個點）、刊頭上的說明字 |
| 只縮不放 | `scale(min(1, var(--z)))`：只有紅色數字 |

縮放只是把畫面拉近看，**不會改變印出來的東西**。

**刊頭上也拖得動畫面**（2026-08 改）。落版 的平移在捕獲階段就把事件吃掉了，所以
它有一串「這幾樣不歸平移」的選擇器；`.masthead` 本來整塊在裡面，而刊頭常常是一塊
版位裡最大的一片 —— 手放在版面正中間拖，畫面完全不動，看起來就是「拖畫面壞了」。
現在它跟格子走同一條路（`pending`）：**手沒動＝點它**（開檔案選圖）、**手一動＝拖畫面**。
真正「抓著這個東西」的那幾樣自己在上面擋：`.mh-grab`（拉長度）、`.mh-move`（⋮⋮ 換位置）、
`.mh-x`、`.mh-zone`（四個落點）、`.blk-grab`（版位那兩條邊）、`.grip`、`#blockHead` 上的欄位。

**三頁都是滾輪縮放**（2026-08 補上 總體）。總體 的倍率是**乘在 fitBoards 算出來的
「符合視窗」上面**（`totalZoom`，1 ＝剛好塞滿），不是直接給 px：兩張版要永遠照同一個
比例並排，給 px 的話換一個視窗大小就對不起來。釘游標下那一點的做法跟 落版 一樣是
**量出來再修正** —— 這一頁的尺規 30px、工具列、中間兩道 34px 的縫都不跟著縮，
用「以捲動原點等比例放大」那個模型算一定會偏。Ctrl +／−／0 也是同一組。
⚠️ `#totalScroll` 的置中要寫 **`justify-content:safe center`**：flex 的置中在溢出時是
往兩邊溢的，放大之後左邊那一截會**捲不到**（`scrollLeft` 已經是 0，畫面上卻是從
第 34 公分開始）。
⚠️ `#totalRail` 要 **`position:sticky` 兩軸都釘**（`left` ＋ `top`）：它站在 `#totalScroll`
裡面，只釘 left 的話一往下捲工具列就上去不見了 —— 而那正是最需要它的時候
（拉近看就是為了切一刀）。

**切進 落版／拼板 一律 `zoomToFit(true)`、切進 總體 一律回到 1 倍**（三邊的 `mount()`）：
第一眼永遠是整張版／整本，跟重新整理看到的那一眼一樣。以前只有第一次看到那個版位才 fit、之後切分頁只置中並留著
你調好的倍率 —— 但兩頁的可用寬度差一整條意見欄，留著的那個倍率到了另一頁多半是溢出
或縮成一小塊，於是每次切過去都要自己再按一次 Ctrl+0。

**左邊那條藍的（`#toolRail`／拼板 的 `#typeWrap`）自己會捲**：字級十幾列＋工具接在下面，
畫面矮一點就到不了最下面那幾顆，而一條齊頭齊尾的藍完全看不出下面還有東西。
拼板 捲的是整條 `#typeWrap`（工具是它的下半截），所以 `#typeWrap #toolRail` 要
`overflow:visible` —— 一條直條裡兩個捲軸只會兩邊都難按。

刊頭上的說明字本來是固定大小，2026-07 改成跟著縮放：那行字要看起來像「印在版上」的，
縮小了看不清楚就自己放大 —— 跟看真正的印刷品一樣。

## 資料

**兩層：`localStorage` 是本機快取，Supabase 是共用的那一份。** 開機先用 `localStorage` 秒開，再拉一次遠端；存檔照舊寫 `localStorage`，另外 debounce 上傳（`PlanSync.queueUpsert`）。後端不在時 `PlanSync.ok()` 回 false，整支走純 `localStorage`。

> ⚠️ **用瀏覽器直接開這幾支＝在正式資料上按按鈕。** 測試一律 `PM_NO_SYNC=1 node dev.mjs`，或用 `driver.mjs`／`scenario.mjs`（自己會擋）。

### 三個庫，各管一件事

| 庫 | 住哪 | 誰在改 | 怎麼進到版面上 |
|---|---|---|---|
| **檔期**（`plans`） | Supabase 專案 A | 行銷開、設計切、PM 挑 | 就是版面本身 |
| **商品目錄**（`catalogue` 一列） | 專案 A | 誰都可以（`product.html`） | 挑貨面板 → 格子裡的品名、規格、價格 |
| **品牌 Logo**（`companies`＋`logos`） | **專案 B**（`logo_page` 自己那個） | 只有設計 | 品牌名對得起來 → 格子左上那張 logo |

**接點只有一個：品牌名。** 商品那一列寫 `HITACHI`，Logo 庫裡有一家叫 `HITACHI`（或 `alt_name` 是它），
那一格就畫得出那張圖。比對前先正規化（大小寫、空白、全形空白、`.`／`-`／`_` 都不算差別，
`PlanSync.normBrand`），`alt_name` 讓同一家公司的兩種寫法（`Panasonic` ↔ `國際牌`）指到同一張。
對不上的品牌在商品目錄那一頁最上面用一條琥珀色的列點名 —— 版面上它會畫成空框，
而「為什麼這個牌子沒有圖」是這兩個庫之間**唯一**會對不起來的事，值得有一個固定的地方講。

- **Logo 庫平常只讀**（`PlanSync.pullLogos()`）：主程式開機拉一次，做成 `品牌 → 公開網址`，
  跟目錄一起發給每一塊畫布。不叫那一頁自己報上來 —— 那樣要等有人打開它才有得用，
  而版面一載進來就該畫得出 logo。拉不到就整個功能安靜地關著。
- **唯一會寫進 Logo 庫的路：把一張圖拖到版上那張 logo 上**（`PlanSync.uploadLogo()`，
  2026-08 加）。排版的人在版上就看得出「這個牌子的圖不對」，而他手上就有對的那一張 ——
  叫他記得再開一次 Logo 庫、找到那一家、刪掉舊的再傳一次，中間每一步都會忘，
  而且忘掉的結果是**別本 DM 還是舊的那張**。所以它不是存在這一格、也不是存進商品目錄。
  **只有設計換得動**（跟刊頭圖同一條理由：那是印出去的素材），身分在主程式那邊擋。
  ⚠️ 那邊一家公司只能有一張活著的 logo（partial unique index），所以「換」＝
  **先把舊的丟進回收桶、再插新的**：順序倒過來會撞索引，而收了舊的又插不進去
  這家公司會變成沒有圖，所以插失敗要把舊的放回來。
  ⚠️ 公司找不到就開一家新的 —— 商品目錄裡打得出這個牌子，就表示那家公司真的存在。
  ⚠️ 換完要**兩種寫法一起更新**（`Panasonic` ↔ `國際牌`）：商品目錄裡打哪一種都算數。
  ⚠️ 有人以前手動傳過的品牌圖排在庫前面（見 `bimgOf` 那三層），所以換完要把那一張
  override 清掉，不然剛剛拖進去的那張永遠看不到 —— 看起來就是「換不動」。
- **版上點一個空框＝把那個庫開起來**：商品圖的空框開商品目錄、品牌的空框開 Logo 庫
  （畫布 `openLib` → 主程式 `openApp()`）。空框講的就是「這裡少一張圖」，而那樣東西
  不在這本 DM 裡。⚠️ 掛的是 **mousedown 記住、mouseup 再問**，不是 `click`：
  按下去那一下就會重畫整格，按住的那個節點當場被換掉，瀏覽器的 click 於是落在
  共同祖先上，`closest('.blk')` 什麼都找不到。
- 為什麼**沒有把兩個 Supabase 專案併成一個**：搬一次要動它的資料、RLS 和網址，
  而這邊要的只是「哪個品牌有圖、圖在哪」，加上換圖那一條寫入路 —— 都是幾個 REST 呼叫。
- 品牌圖三層，前面的贏：後台照品牌名上傳的 > 後台照商品名上傳的（舊資料）> **Logo 庫**。
  手動排前面是刻意的：有人特地換過一張，那就是他要的那一張。庫是「沒有人特別指定時的正確答案」。
- ⚠️ **Netlify 的站要指名**：`famous-buttercream-edc32c` 是這個專案的，`jocular-clafoutis-8c7671`
  是 `logo_page` 的。這台機器上的 `~/.netlify/state.json`（家目錄那份全域設定）指著後者，
  所以在 pm_page 裡不指名就會發到 Logo 的站上去 —— 8/09 真的發過去過。
  兩個資料夾各自的 `.netlify/state.json` 已經寫死正確的 siteId（`.netlify/` 不進版控，
  所以換一台機器要重做一次）。
- ⚠️ `logo_page/` 從 CDN 載 `supabase-js`（跟這裡「零相依」那條不一樣）。
  那是它自己那一頁的事：斷網時它開不起來，主程式不受影響。
  `build-single.mjs` 只搬它的 `index.html` 和 `config.js` —— 整包複製會把它的
  `schema.sql` 和本機的 netlify 快取一起發出去。
  ⚠️ **Logo 那個專案在 free tier 上會被暫停**，一暫停子網域就整個解不到 DNS
  （看起來像被刪了）。那時候品牌圖會全部變空框 —— 這正是「該把兩個 Supabase
  合成一個」的理由，合併的做法見下面那一段。
- **把 Logo 那個專案合進來**（還沒做，卡在一步 DDL）：
  1. pm_page 那個專案的 SQL Editor 跑 `logo_page/schema.sql`（DDL，anon key 跑不動，
     只能在後台做）。跑之前先確認 `storage.objects` 上沒有同名的 policy：
     `select policyname from pg_policies where schemaname='storage' and tablename='objects';`
     —— 那四條 `anon % logos` 是整份腳本唯一碰到共用東西的地方。
  2. `node logo_page/migrate-to-pm.mjs` 先預演、`--go` 才真的搬。
     ⚠️ 不能單純複製 `logos.path`：它是 `<company_id>/<uuid>.<ext>`，而 A 那邊的 25 家
     是 seed 出來的、id 不一樣，所以要照名字把公司對起來再用新的 id 重組 path。
  3. 兩支 config 指到同一個專案：`logo_page/config.js`、pm_page 的 `LOGO_SUPABASE`。
  （2026-08-11 清點過：25 家活著的公司就是 seed 那 25 家，真正要搬的只有 Panasonic
    那一張圖；回收桶裡那兩家是測試垃圾，不搬。）
- ⚠️ **商品目錄存的是「整包」**（`rows` 就是全部），所以「畫面上是空的」時存一次＝把全公司的商品刪光。
  主程式那邊有 `catReady`（共用目錄拉完了沒），那一頁讀到之前**完全不存**（連本機快取都不寫）。
  空白有兩種：「真的是空的」和「還沒讀到」，長得一樣，差一整份公司資料。

### 貼紙（版上叫「貼圖」）

**第四個庫，只活在這台瀏覽器**（`sticker_editor.html`，localStorage 的 `stk.db`／`stk.assets`）。
跟品牌 Logo 同一類：版面上可以貼的素材，所以**只有設計看得到、也只有設計放得上去**。
兩個入口開的是同一張浮層（`?embed=1`）：檔期列表上那顆「🏷➕ 貼紙庫」，和貼紙面板底下那顆。
⚠️ 這裡的「貼紙」跟意見欄那疊便利貼是兩回事（那邊也叫貼紙）—— 工具鈕上寫「貼圖」就是為了這個。

- **挑貼紙的那一片站在左邊那一欄**（`#secStk`，2026-09 改，照 Canva 那種「左邊一排圖示 →
  旁邊一整片素材」）。拿起 拼板 的 貼圖（🏷️，快捷鍵 T），左欄就從「檔期清單／整本落版」
  換成搜尋 ＋ 分類 ＋ 最近用過 ＋ 全部貼紙。
  ⚠️ **為什麼不是浮層**：216px 的盤子蓋住的正是你要貼的那幾格（取色盤當初站到工具列
  左邊就是這個理由）。接管那一欄還有一個好處 —— 版面一點都不必重排。
  ⚠️ `at-stk` 這個 class 由 `syncStickerTool()` 自己加，**不要寫進 `paintSideMode()`**：
  那一支在 `hostSide()`／`setSideMode()` 很早就會被叫到，而 `activeTool` 是底下才宣告的
  `let` —— 在它上面讀就是 ReferenceError，整支程式停在那一行。
  「最近用過」＝`plan.stkUse`（這一檔貼過幾次），跟 `plan.bgUse` 同一種**只會加**的東西：
  不進 `META_KEYS`，合併走 `mergePaintInto()` 取兩邊各自的增量。
  只在沒搜尋、沒挑分類的時候出現 —— 篩過之後它跟下面那一片講的是同一批東西。
  **縮圖拖得到格子上**（2026-09 加），跟「點一張拿在手上、再點一格」兩條路都通；
  兩條最後都進 `pasteStickerOn()` —— 各寫一份的話「還沒挑貨」「不是設計」
  「最近用過」遲早有一邊漏掉。
  ⚠️ **拖曳是自己接指標事件，不是瀏覽器原生那一套**（便利貼那一疊用的是原生的）。
  兩個理由：原生拖曳從 `<button>` 起不來，而縮圖就是一顆按鈕；而且拿著 貼圖 的時候
  格子裡的畫布是**活的**（見下一條），原生拖曳會掉進那份文件裡，外面永遠等不到放手。
  指標捕捉沒有這兩個問題 —— 按下去之後每一下都送回那顆縮圖，蓋在上面的是誰都沒差。
  ⚠️ **拖到一半不可以重畫那一片縮圖**（`markStkArmed()` 只換那一圈藍框，
  不叫 `renderStickerPanel()`）：重畫等於把正被捕捉的那顆節點換掉，捕捉當場失效，
  手上那張跟著游標的縮圖停在原地、放手那一下永遠等不到 ——
  畫面上就是「拖了半天什麼都沒發生，還多一張浮著的圖」。踩過一次。
  手上帶著一張 `.stk-ghost`（跟著游標走），經過的那一格照便利貼那一套亮起來（`hotCell`）。
- **拿著 貼圖 的時候，格子裡的零件照樣搬得動**（＝ S 那顆的本事，2026-09 改）：
  `setTool()` 不給它加 `.tooling`，所以畫布照樣收指標。按在**零件**上就是搬那個零件，
  按在格子的**空白處**才是貼一張上去 —— 那一下畫布會轉出來（`stagePass` 補的是格子的
  `click`），所以貼上去那一段接的是 `boardOv` 的 click，**不接 pointerdown**
  （上色、取色那幾支是接 pointerdown 的，它們把整片畫布關掉了）。
  ⚠️ 兩邊各接一份的話，會有一種按法貼得上、另一種按不動。
  關掉畫布的下場是**剛貼上去那張自己也搬不動**，得先換回 S 再換回來。
- **貼上去之後那幾個字還改得動**（2026-09 加）。點一下選起來、再點文字就地打字，
  Enter 或點別處就存。存的是 `b.vals`（**這一格自己**的字），模板不動 ——
  所以同一張貼紙貼十格可以寫十種價格，那正是貼紙庫「可變文字」的用處，
  而攤平成一張圖就完全沒有了。
  ⚠️ **兩下不是一下**：`.ft` 平常 `pointer-events:none`，選起來（`.blk.sticker.sel`）
  才點得到。不然第一下就掉進字裡面，那張貼紙永遠搬不動。
  ⚠️ 主程式那邊的 mousedown 要**放行**「按在已選起來的貼圖的字上」那一下，
  畫布那邊同理 —— 不放行的話那一下被當成「抓著它搬」，整格當場重畫，
  按住的節點被換掉，游標永遠進不到字裡面（跟平移那幾條同一個坑）。
  ⚠️ 打字中**不重排**（只有離開才 `renderCell()`）：重排＝整格重畫，游標當場不見，
  第二個字就打不下去了。形狀跟著字長是離開時算的。
- **一張貼上去的貼紙帶著三樣東西**：`libId`（庫裡哪一張）、`tpl`（模板整份抄一份）、
  `vals`（這一格改過的字），外加它用到的 `assets`（只帶用得到的那幾張圖）。
  **為什麼模板整份跟著走、不是只記一個編號**：貼紙庫只活在這台瀏覽器，別台電腦打開
  這本 DM 的時候庫裡沒有那一張。代價是庫裡改過的不會回頭改已經貼出去的 ——
  跟品牌圖「手動指定的排在庫前面」同一條理由：有人特地擺上去的，那就是他要的。
  `svg`（烤好的那一張）還留著當備胎，而且**這個功能之前貼上去的就只有它**，
  那一種照樣看得到，只是改不了字（見 `stickerArt`）。
- **畫法抽成一支共用的檔案**（`sticker-render.js`，零相依、`<script src>`，跟
  `supabase-sync.js` 同一種穿法）。
  ⚠️ `build-single.mjs` 的清單要有它 —— 漏掉就是站上一頁死掉（`StickerRender is not
  defined` 發生在整支程式最上面），而**本機直接開檔完全看不出來**（檔案就在旁邊）。
  `--bundle` 那條路會把它內嵌進兩支裡面：寄出去的單檔旁邊沒有第二支檔案。
  ⚠️ 版面用到的那幾條 CSS（`.lyr`／`.lbl`／`.ft`）兩邊各有一份，那是刻意的：
  幾行定位規則改壞了在自己那一頁一眼就看得到，真正會悄悄走鐘的是那幾百行怎麼排。
  ⚠️ **`.lyr` 是貼紙那一疊圖層的名字，別的地方不要再用**（2026-09 撞過一次：
  畫布那一頁右邊的圖層清單，每一列本來也叫 `.lyr`）。撞名不會讓版面亂掉，
  它做的是把清單那一列的內距和框線悄悄套到每一層貼紙上 —— 而貼紙的字級是照
  **整張貼紙多大**算的，框卻被擠小了十幾個**不隨貼紙縮放**的點，於是字對它自己的框
  永遠偏大；一擠過臨界點，「48」就折成上下兩行整個爆出紅圈外，看起來像貼紙做壞了。
  那一列已經改叫 `.lrow`。
  ⚠️ **貼圖不吃 `.blk` 那一圈內距和框線**（`.blk.sticker{padding:0;border-width:0}`）：
  那一圈是給文字零件不要貼著框線用的。留著的話畫進去的框比 `b.w` 小十幾個點、
  字級卻是照 `b.w` 算的，而且橫向和縱向被吃掉的量不一樣（圓會變成橢圓）。
  手放上去那一圈提示改用 `outline` —— 它不佔位置。
- ⚠️ **只貼得到已經挑了貨的格子**：沒貨的格子根本沒有畫布（`paintOneCanvas` 看的是
  `pickedOf`），貼紙沒有地方站。所以那一下會講一句「這一格還沒挑貨」——
  安靜地什麼都不做的話，看起來就是「貼圖壞了」。
- **貼圖落在你放手的那一點**（不是格子正中間，2026-09 改）：拖進去和點一格貼上去
  走的是同一條（`pasteStickerOn(cel, row, at)`），那一點是它的**中心**，超出格子就往
  裡面收（一半掉在外面的貼圖印不出來，而版上看起來只是「歪了」）。
  ⚠️ **上層送的是「畫布那份文件自己的客座標」，不是排版座標**：中間還差一個
  「裝不下就整份縮」的倍率，而那個倍率只有畫布那邊知道（`scaleOf`／`cellPt`）。
  在上層換算的話，縮過的格子會差一個倍率 —— 量到過：丟在格子左上四分之一，
  貼圖卻貼在左上角。所以上層只除掉 iframe 上那個 transform（`canvasScale`），
  剩下那一半在 `addStickerBlock` 裡用 `cellPt` 補完。
- **拿掉一張貼圖＝它右上角那顆 ✕**（`.delbtn`／`delSticker()`，2026-09 加）。以前唯一的
  辦法是選起來按 Delete —— 那要先知道有這回事，而且要那一格先收得到鍵盤，按下去沒反應
  看起來就是程式壞了。滑鼠移過去就出現，**選起來也留著**（「最上面」那顆反過來，選起來
  就收掉：那時候要拖、要縮，一顆鈕站在圖上只會擋路）。站在框的外面一點點 ——
  選起來的時候四個角是改大小的把手。
  ⚠️ 刪掉是真的從那一格拿掉，**不進「刪掉的東西」那份名冊**（那份是給商品圖找回來用的，
  靠 uid 認出重選商品時要復原哪一塊，而貼圖不是挑出來的）。要叫回來走 Ctrl+Z。
  只有設計看得到那顆（跟「誰貼得上去」同一條）。

- **圖不進 plan，只存網址。** 商品圖、品牌圖、刊頭圖都走 Storage（`PlanSync.uploadImage`）。base64 塞進 `plan` 會把 `localStorage` 撐爆、每次存檔重傳好幾 MB，還被歷史那 50 份快照各留一份 —— 這三條都踩過。
  **Storage 的檔名＝那個東西的身分，不是使用者的檔名**（上傳是 upsert，同名就是同一個物件）：商品圖用品名、刊頭用 `masthead-<檔期>-<版位>`。用使用者的檔名，兩個版位各放一張 `banner.png` 就會蓋成同一張。
  **換圖要能看得到**：`sheetSig` 只記「有圖／沒圖」，換第二張簽名不變就不重建那個 `<img>` —— 所以換圖時 `sheet.mhRev` 加一，簽名讀它（不讀網址：base64 好幾 MB）。網址失效的圖會自己換成一句「讀不到」，不是一個破圖示。
- `catalogue_plans_v1` —— 整包檔期。版位、格子、便利貼（`cell.notes`）、拼好的畫布（`cell.board.state`）全都在 `plan.layout` 裡面。
- 因為都在 `layout` 底下，`snapshot()` 一序列化就全部有了 —— 存檔和 Ctrl+Z 是免費的。**改了資料要進歷史就叫 `commitAction()`，只想存檔叫 `markDirty()`。**
- **復原留 300 步，而且有畫素以外的另一個上限**（`HISTORY_MAX`／`HISTORY_MAX_BYTES`）。
  一份快照就是整個 plan 的 JSON（排滿的檔期約 143KB），所以真正的成本是記憶體不是步數：
  兩個上限一起夾，先超過哪一個都從最舊的那一頭丟，但**永遠留兩份**（只剩一份就等於
  「沒有可復原的步驟」，而人剛剛才做了一個動作）。
  還能退幾步**直接寫在鈕旁邊**（`#histN`）—— 以前只寫在 `title` 裡，於是「還剩幾步」
  是一件要按到沒反應才知道的事。只寫「還能退幾步」一個數字：能重做幾步是按了復原
  之後才有的事，兩個並排會變成一組要先讀懂的東西。
- **球在誰手上＝時間說的，整本一起走**（2026-08 改）。這本 DM 走到第幾關，是檔期表填的
  `start`＋`durs` 排出來的那條時間軸算出來的（`STAGES`：提明細 → 製作 → 一校 → 一改 →
  二校 → 二改 → 蓋章確認 → 蓋章修改 → 審稿 → 出稿；`autoStageIndex()` ＝第一個 18:00 還沒過的
  那一關，跑完了停在出稿）。
  ⚠️ 2026-08 只**改過名字**（提品→提明細、蓋章→蓋章確認）：關數、誰的回合、
  `durs` 的長度一步都沒動，所以舊檔期不必換算。每一關是誰的事寫在 `STAGE_SIDE`（PM／設計交替，審稿和出稿
  都算 PM）—— **那一份就是全部的答案**，`planSide()` 查它，`myTurn()` 拿它跟自己比。
  **誰都推不動它**：要提前或延後就去改檔期的時程。
  ⚠️ **以前是一格一顆棒子**（`cell.st` ＝ 0..7 ＋ 拼板 的蓋章模式 ＋ 落版 角落那顆
  「送給 PM」＋ 格子右上角那枚寫著關名的標籤），整套連根拿掉。舊檔期資料裡還留著
  `cell.st`／`cell.at`，沒有人讀它（照 `cell.pm` 那條先例：刪一遍要把每個檔期整包
  改寫上傳一次，換來的只是少幾個 byte）。
- **不壓暗、不擋**（`.notmine` 整條拿掉）。整本一起走，所以「不是你的回合」會是整張版
  一起暗 —— 那沒有指出任何東西，只會被當成程式壞了。現在是哪一關寫在橫幅上一行字裡
  （`updateProgress()`：`現在：一改（設計・輪到你）　倒數 2 天・挑好 88/352 格`）。
  **便利貼任何時候都貼得了**：它是對話，不是版面的一部分 —— PM 在 製作 那一關想問一句，
  那正是最該問得到的時候。
- **唯一還在長的進度是「挑貨」**：版位狀態 `blockState()` 改成講**這一塊的貨挑齊了沒**
  —— 綠＝都挑好了、黃＝「挑好 8/14 格」、白＝還沒挑貨、灰＝還沒排版（連格子都還沒生）。
  以前這三級講的是「球在誰手上」，而那件事現在整本一樣，十三塊版位會畫出同一個顏色。
  總體、小地圖、進度面板、檔期卡下半讀的都是這一支。
  （**沒有「鎖定」這回事**，`sheet.confirmed` 更早就整條拿掉了：列永遠搬得動、刊頭永遠
  拉得動、搬移工具永遠可以用。）
- **要看別的關長什麼樣就撥時鐘**：`now()`／`nowMs()` 是整支程式的「現在」（差多少存在
  `localStorage: pm_fake_now_v1`，**不進 plan、不同步**）。三條路，都走 `applyClock()`：
  ・**名牌旁邊那顆 🕒**（`#clockBtn`／`#clockPop`，devtool）——
    打一個日期、或 ±1／±7 天、±3 時（**疊加**，連按兩下＋1 天就是往前兩天）、回到現在。
    它掛在 `#whoBadge` 裡面，所以跟著 `hostWho()` 走遍三條列 ——
    **三個身分、每一頁都有**。三個身分都要給：設計看「輪到你」、PM 看倒數、
    行銷排整條時程，只有一種人測得動的 devtool 等於沒有。
  ・**網址 `?now=2026-08-20T10:00`**（不寫進 localStorage）—— 測試一關一張圖就靠它。
  ⚠️ 便利貼的時間戳（`stamp()`）**不走** `now()`：那記的是「這句話什麼時候說的」。
  ⚠️ 撥開的時候 🕒 那顆變琥珀色、直接把日期寫在鈕上 ——
  沒有那個記號的話，過兩天回來滿畫面的日期都不對，第一個念頭會是資料壞了。
  ⚠️ **別在函式裡宣告 `const now = ...`**：會遮掉全域那支 `now()`，之後每一次呼叫都是
  TypeError（`renderPlans` 裡那個區域變數因此叫 `today`）。
  ⚠️ `#clockAt` 的 change 要擋掉「跟現在一樣」那一發：按完 ±N 天之後那個欄位是程式
  填回去的，而 datetime-local 失焦時還會補一發帶著舊值的 change，會把時間拉回去。
- **審核通過**（`cell.pass`，2026-08 加）。**一校／二校／審稿**（`REVIEW_STAGES`）那三關，
  正在審的那個人在 拼板 的**意見欄、貼紙那一疊底下**多一顆大鈕（`#passBtn`，快捷鍵 A）：
  **點一格＝這一格過了、再點一下取消；拉一個框＝整批過（Alt＝整批撤掉）**。
  手勢整套照 🎨 抄（同一個 `.sel-box`、同一支 `paintCellsIn`）—— 審一個版位就是一路刷過去。
  ⚠️ **只有「點一格」是切換**：蓋錯一格是最常發生的事，而按著 Alt 再點等於要先知道
  有 Alt 這回事。拉框不切換 —— 一片格子有的過了有的沒過，逐格反轉的結果沒有人預測得到。
  怎麼用寫在 `title` 裡（滑過去才出現），鈕上只有關名：那一行說明只有第一次要看，
  而它天天都在。
  **它不在左邊那條工具列裡**：校對那幾關這是那個人整頁要做的那一件事，收進工具列
  等於要人先去找它；而 PM 在 拼板 本來連那條工具列都沒有（`syncTypeBar` 擋掉的）。
  長相照以前那顆「換手鍵」（綠色、滿寬、圖示＋兩行字），**開著的時候整顆反白** ——
  它是一個模式（現在點格子的意思跟平常不一樣），那件事要一直看得見。
  **看得到那顆鈕的人＝`myTurn()`**（`reviewing()`）：那三關的 `STAGE_SIDE` 都是 pm，
  所以不必再寫第二套權限，而且撥時鐘撥走那顆鈕自己就消失（球在誰手上是時間說的）。
  **但標記誰都看得到** —— 設計要知道哪幾格過了，那正是這件事的用處。
  ⚠️ **資料永久累加、版上一次只寫一枚**：一關一筆（key 就是關名），存的那一份
  是簽核紀錄，不會被下一關洗掉；但格子上那枚標籤只寫**走到哪一關**（`passNames`
  的最後一個），不是「一校通過・二校通過」整串。整串疊在一格 3.7cm 寬的格子上
  就是一條橫過整格的標籤，而「二校過了的一定也過了一校」照關的順序本來就自明。
  完整那一份在 `title` 裡（幾關、誰蓋的、什麼時候）。
  撤銷不看是誰蓋的（三位 PM 對整本 DM 一視同仁）。
  ⚠️ 畫成 `.cell` 上的 `data-pass` ＋ `::after`（**不塞文字節點** —— 那一批 `.cell` 會
  原封不動被拿去當畫布的容器），另外有一支 `paintCellPasses()` 從資料重刷，掛在
  `paintCellBgs()` 同樣那幾個出口上（Ctrl+Z 退掉的可能就是一次蓋章）。
  ⚠️ 它是**標記不是印出去的東西**：固定大小，列印時洗掉（`#board .cell::after` 那一條
  本來就無條件關掉它）。
  ⚠️ 合併**要往下合**（`mergePass()`，跟便利貼並列）：三位 PM 同時在審，各點各的格，
  「一格用我的」會把對方剛蓋的丟掉。照關名取聯集、**只有我撤掉的才算撤掉**，
  而且 `stripNotes()` 要把 `pass` 也扣掉，不然每次合併都報一個假衝突。
  ⚠️ 平移那條要放行（`activeTool === 'pass'` 也要 return），不然框選會變成拖畫面 ——
  跟上色同一個坑。
- **沒有「這一格找誰」這回事**（2026-08 拿掉）：三位 PM 對整本 DM 一視同仁，每個人都看得到、
  點得進去、改得動。
  以前是一格一位 PM（`cell.pm` ＋ 拼板／落版 的「指派」模式 ＋ 格子右上角那個「・甲」
  ＋ 小地圖藍點 ＋ PM 左欄的「我的品類」），整套已經連根拿掉。
  **舊檔期上的 `cell.pm`／`sheet.pm` 沒有去刪**：留在資料裡沒人讀就好，刪一遍要把每個檔期
  整包改寫上傳一次，換來的只是少幾個 byte。
- **總體 有自己的小工具列**（`#totalRail`，設計和行銷都看得到；PM 沒有這一頁）：**選擇（預設）／品類／切分／移動／刪除**，
  照行銷那一支的 點選／切割／互換 同一組圖示和規矩 —— 兩邊來回的人不必記兩套。
  預設那個工具**什麼都不改**：這一頁動的是整本 DM 的骨架，拿起 切分／移動 要是一個刻意的動作。
  **切分整套照行銷開檔那支搬過來**（原本在 `落版單系統.html`，已併入）：指到哪一塊就框紅（`cutAim`）、
  滾輪切直／橫、最上面那條「切在 X__ Y__ cm｜切下去」（填了就照座標切，空著照點下去的位置）。
  **座標＝那一張版上的第幾公分，跟上面和左邊那兩條尺規同一把尺**（`cutCoordPoint()`）——
  所以「要切哪一塊」是座標自己算出來的（`nodeElAt()`），紅框會跟著跳過去，紅線的讀數寫的也是
  同一個座標。以前那個欄位是「離**指到的那一塊**的邊幾公分」：那要先知道那一塊從哪裡開始，
  而版上唯一寫著數字的東西就是尺規。
  ⚠️ 兩張版並排，座標自己講不出正面還是背面 —— 用紅框那一塊在的那張，沒有紅框就用正面。
  所以滑鼠還是有用，只是它挑的變成「哪一張版」。
  **填 X ＝直切、填 Y ＝橫切**（那兩個欄位的 `input` 自己叫 `setCutDir()`）：X 是「從左緣量起
  第幾公分」，那個數字唯一畫得出來的線就是一條直線 —— 填完還要再滾一下滾輪，等於那個欄位
  只講了一半。滾輪和那兩顆方向鈕照舊，那是刻意換方向。
  只填一軸也走得通，**沒填的那一軸不拿游標補**（那會讓同一個數字因為滑鼠停在哪而切到不同
  的版位）：先用紅框那一塊，沒有紅框就沿著填了的那一軸找第一塊碰得到的（`nodeOnAxis()`），
  而且它只用來挑「切哪一塊」，那一刀的位置永遠是填了的那個數字。
  游標也跟著換：寫著「直」／「橫」的那兩顆 SVG（`--cursor-v`／`--cursor-h`）。
  內容留在第一半、第二半是空的（`未命名` 0 支）；
  移動＝拖到另一塊，換的是**內容**（品類、格子、刊頭），大小和比例不動（版位的比例是落版談好的）。
  **🗑 刪除**＝點一塊就把它從版上拿掉（`deleteBlock()`／`askDeleteBlock()`）——
  切分 的反面。旁邊那一塊**補上它的位置**（也就是「那一刀沒有切過」），不是留一個洞：
  一張 DM 上沒有洞這種東西。已經有格子或品類的先問一句（`modalConfirm`），空的直接拿掉；
  兩條路都走 `commitAction()`，Ctrl+Z 退得回去。
  ⚠️ 爸爸只剩一個小孩時要把爸爸**整個換成那個小孩**，不能留一個只有一個小孩的容器：
  畫出來一模一樣（`effSizes` 算它 100%），但下一刀切在它身上就多包一層，
  而且那條切割線拖不動（只有兩個小孩之間才有 `resizer`）。
  ⚠️ 剩下那幾塊的比例要在**拿掉之前**量：拿掉之後 `effSizes` 會把空出來的百分比全部
  算給 `size` 是 null 的那一個（它是 `flex:1`），於是空間全歸最後一塊。
  ⚠️ 一面只剩一塊時不給刪 —— 拿掉它這一面就沒有版面了。

  兩個都走 `commitAction()` → `rebuildAll()`，所以 Ctrl+Z 退得回去。拖切割線調比例不歸工具管 —— 連續的量哪個工具都該拖得動。
  它是 `#view-total` 的第一個小孩，所以重建版面只清 `.board`（`clearBoards()`，以前是 `innerHTML=''`，一重建工具列就沒了）。
  **✏ 品類**（`#itemPop`）＝點一塊，填它要放哪些品類、各幾支，就是開檔那支「點選」做的事。
  底下那份**品類清單**（`#catMenu`／`renderCatMenu()`）是**看得到的一份、點一下就填進去**，
  從行銷那一支搬回來的 —— 併進編輯器的時候它變成瀏覽器的 `datalist`，而那個要先打對字
  才掉得下來，等於「你得先知道有什麼」才找得到它（真的有人因此以為填不進去）。
  來源三份接起來去重（`catOptions()`）：**這個檔期用過的** → **商品目錄的類別**（`row.cat`）
  → **內建的 `CATEGORIES`**。內建那份是墊底：目錄還沒拉到、或那邊的列還沒分類別時，
  清單不該是空的（空的等於沒有這個功能）。
  ⚠️ 它是**提示不是自動完成**：沒有反白、沒有上下鍵、Enter 不會替你選，打什麼就是什麼。
  藍色在這個專案是「你正在點的東西」，一個還沒被點過的建議不該長那樣。
  跟你打的字一模一樣的那一筆不列，一筆都不剩就整條收起來（`#catMenu:empty`）。
  ⚠️ 選一筆掛的是 **mousedown**，不是 click：click 的話輸入框先失焦、選單已經重畫過了。
  **打完按 Enter ＝按那顆 ＋**（支數空著算 1）：上面改名、改支數兩欄 Enter 都算數，
  只有「加一個新的」那一列非得用滑鼠點不可 —— 一塊版位要填好幾個品類，
  按了沒反應看起來就是「填不進去」。
  `datalist`（`#catList`）留著給已經加進去那幾列的改名欄用。
  支數是格子的來源（`buildCells` 照 `groups[].count` 生格），所以**改支數＝加減格子**：
  加的接在那個品類最後一格後面、減的從最後一格拿掉（`setGroupCount()`），**已經拼好的那幾格不動** ——
  不要重生一份 cells，那會把挑好的貨、便利貼、走到第幾關全洗掉。改完 `sh.rows` 要清成 null
  （釘住的列數對不上新的格數），版位的 `name` 也要跟著品類走（落版 的標題讀的是它）。
  最後一個品類被拿掉就留一塊「未命名 0 支」的位子 —— 整塊空白看不出是「還沒填」還是「畫壞了」。
  ⚠️ **那個位子不是一個品類**（`isBlankGroup()`／`realGroups()`）：不列、不數、不給刪。
  以前它會照樣畫成一行「未命名」，跟隔壁那行「刮鬍刀(12)」長得一模一樣，而在 ✏ 浮層裡
  更是一整列可以改名、可以改支數、還有一顆 ✕ 的東西 —— 看起來就是有一個叫這個名字的品類。
  現在版上寫「還沒填品類」（`.unfilled`，淡一階、細一點，一眼看得出是一句話不是一項），
  浮層標題也是同一句。**`gi` 還是照 `node.groups` 的真實編號**，不是畫出來的第幾列 ——
  `setGroupCount()` 照那個編號去找格子。
  沒有這一顆之前，設計切出一塊新版位就得回行銷那一支才補得上內容。
- **總體 的單位是公分，不是「尺」**（2026-08 改）。`DM_SIZE` 照 `plan.type` 查這張版真正幾公分
  （全省對開大DM＝78.7×54.5），尺規一格一公分、寫數字的那幾條長一點，游標讀數、對齊線、
  切分的長度、切割線上打的數字全都是 cm。（以前開檔那支有自己的一份 `DM_SIZE`，改一邊要改兩邊。合併之後只剩這一份了。）
  以前是 `CONFIG.lengthDivisions/sideDivisions`＝20×10 格的「尺」，那個尺既不是台尺也不是公分，
  而版上其他東西（刊頭、列高）本來就講公分 —— 同一頁上不該有兩把尺。
  舊檔期的對齊線存的是舊格數，第一次開起來會換算並蓋 `plan.guidesCm` 這個章**當場存檔**
  （不存的話，開了沒改就離開，下次會再乘一次，線就跑到版外面）。
  **版子畫出來的比例也照公分算**（`boardPx()`：寬固定 742，高＝742×h/w）。以前寫死 742×525＝1.413，
  真正的對開是 1.444，差 2%：橫的一公分和直的一公分在螢幕上不一樣長，`blockAspect()` 量出來的
  「這個版位幾公分高」也跟著偏。小地圖那張紙同樣照這個比例（`miniPageH()`）—— 它是同一張版的縮圖，
  比例不一樣的話同一個版位在兩邊會長得不一樣胖。量長度還是分軸（`pxPerCm(el, vertical)`），
  那是保險，不是因為現在兩軸不一樣。
- **左邊那一欄：三個身分、三個分頁都一樣，而且一次只看得到一段**（`#pmStack`）。
  `at-list` ＝檔期清單，`at-map` ＝那一本的整本落版。**先挑一本 DM，才看得到它的版**
  （`sideMode`／`setSideMode()`／`paintSideMode()`）。兩段上下疊著的那一版，286px 裡
  上下各半截，清單看不到幾檔、小地圖也擠不下兩張版 —— 而它們本來就是先後關係，
  不是並列。所以段標題不再是開關（`pm_side_secs_v1` 那套收合整組拿掉了）。
  以前只有 PM 有、只在 拼板 有；設計只看得到光禿禿的小地圖，換一檔得退回檔期列表。
  同一個畫面上同一個位置給不同身分不同的東西，只會讓人以為自己那一份壞了。
  （`pmStack`／`renderPmPlans` 這些名字裡的 pm 是歷史。）
  整欄是**同一顆** DOM，三個分頁搬來搬去（`hostSide()`，跟 `hostSheet()`／`hostWho()`
  同一個做法），永遠站在那一頁的最前面；小地圖（`#sheetSide`）掛在第二段裡，
  同一份 `renderMinimap()`，不養第二套。外緣那根書籤收的是整欄。
  **三個分頁共用同一個 `sideMode`**：挑了一檔就是挑了一檔，切個分頁不該回到清單 ——
  那會讓人以為自己剛剛挑的沒算數。
  ⚠️ **總體 上點小地圖只是「挑一塊」**（`switchBlock()` 在 `activeTab==='total'` 就
  `selectTotalBlock()` 然後 return）：`#sheetPane` 這時候停在別的分頁上、量不到寬，
  照常 `renderGrid()` 會拿 0 算出一份尺寸全錯的版，而且被 `sheetSig` 記下來。
  真的要畫是切到 落版／拼板 時 `mount()` 的事 —— 挑好的那一塊會跟著過去。
  （所以 總體 的 `mount()` 也要 `renderMinimap()`：那一段在這一頁一樣看得到。）
  ⚠️ **總體 的版面收在 `#totalScroll` 裡自己捲**（`editorHost`／`totalHost` 都指它，
  「這一頁是紙」的底色也在它身上）—— 捲的是那張紙，左欄不該跟著跑掉。`fitBoards()`
  量的也是它，而且要扣掉小工具列和**兩道** 34px 的 gap（工具｜正面｜背面）。
  第一段就是首頁那份檔期清單的窄版（`renderPmPlans()`）：**換一檔不必退回列表**（`switchPlan()` → `openPlan()`，
  寫入還卡著會先問）。右邊那行寫「還沒落版」或「n 格」。點一檔＝順手翻到 map。
  **「← 檔期列表」（`#backBtn`）站在這一段的最上面**：這一段講的就是「你在哪一本 DM」，
  離開這一本是同一件事的最後一步。以前它自己佔一條藏在螢幕上緣的藍色橫列（`.modebar`），
  那條列上其他東西早就搬走了 —— 為了一顆鈕要人把滑鼠推到頂端，而在檔期列表上它整條是空的。
  中間本來還有一段「我的品類」，跟著一格一位 PM 一起拿掉了 —— 沒有那件事之後它只會列出
  整本 DM 的每一個品類，跟第二段那張小地圖講同一件事。
- **搬列那根 ⋮⋮（`.grip`）只在 落版**（`#boardOverview .grip{display:none}`，2026-08 從
  「只擋 PM」放大到擋所有人）。拼板 排的是格子裡的貨，列怎麼排是上一頁談好的事；
  而它在 拼板 不只是多一顆按鈕 —— 它是每一列左邊固定 20px（14＋6px margin）的一條，
  真的佔掉版面的寬度，整張版看起來往右歪一截。
- **落版 最上面那一列＝這一塊的尺寸**（`renderHead()`，2026-08 加）：
  `刮鬍刀　版位 [19.5] × [29.9] cm　刊頭 19.5 × [13.9] cm　14 支　列數 [3]`。
  - ⚠️ **打得進去的那幾格平常就要長得像欄位**（`.mh-num`，2026-08 改）：白底、
    一圈框、深字。以前它是透明底、透明框、灰字，只有滑鼠碰到才長出框 —— 於是
    版位的寬高、刊頭高度、列數這四個真的能改的數字，看起來全是標題上的灰字，
    沒有人會去點。**改不了的那一格才退回灰字沒有框**（`:disabled`），
    跟旁邊那個本來就是讀的「刊頭寬」（`.mh-fixed`）長得一樣 ——
    能動的看起來能動、不能動的看起來不能動，兩邊都要成立。
  - **版位的寬和高打得進去**（`addBlockCmFields()` → `setBlockCm()`）。動的是**那一刀的
    比例**：從這一塊往上找第一個沿著那條軸分的容器（`axisOwner()`），旁邊那幾塊照原來
    的比例讓出空間 —— 整張紙多大是事實，不會因為誰變寬就跟著長大。
    改不了的那一軸欄位反灰，`title` 裡寫**為什麼**（「這一塊就是整張版的寬」）：
    寫著卻按不動比沒有更難懂。剩下的只有這一種：那一軸上根本沒有第二塊。
  - **也拖得動：右邊那條邊改寬、下面那條改高**（`.blk-grab`／`startBlkResize()`，
    2026-08 加）。手勢照刊頭那條把手抄（同一個讀數泡泡、同一個 rAF 重畫、
    放手才 `commitAction()` ＋ `rebuildAll()` 記一步）。改不了的那一軸不畫把手 ——
    畫著卻拉不動跟寫著卻按不動是同一件事。**只在 落版**（`#boardOverview` 收掉）。
    ⚠️ **把手要看得見**：一開始是邊上一條 3px 的細線，跟版面的外框分不出來 ——
    「知道它在那裡才找得到」等於沒有。現在整條邊是一條淡藍的線，中間掛一顆
    **寫著現在幾公分的牌子**（`.blk-lab`：`↔ 39.2cm`／`↕ 29.9cm`）：那顆牌子一次
    說了三件事 —— 這裡可以抓、抓了會改什麼、現在是多少。抓的是**整條邊**，
    牌子 `pointer-events:none`。牌子是固定大小（螢幕上的記號，不是印出去的）。
    ⚠️ 牌子擺在紙的**外面**，但版面平常就是塞滿窗口的（`gridW ＝ availW`），
    所以右邊那顆多半會掉出窗口 —— `ensureBlkGrabs()` 量一次、掉出去就加 `.flip`
    翻進來貼著邊站。被切掉一半的牌子比壓在一格上面更難懂。
    ⚠️ **拖的時候尺度要釘住**（`blkScalePin`，`sheetMetrics` 裡 `gridW` 那一段）：
    平常 `gridW ＝ 這塊窗口的寬`，所以版位變寬螢幕上不會變寬（只有高度跟著縮）——
    你拉的那條邊完全不動，手上和畫面上是兩件事。釘住之後兩條邊都跟著手走 1:1，
    放手才回到「塞滿窗口」（那一下的尺度變化跟按 Ctrl+0 是同一種）。
    ⚠️ 平移那條要放行（`.blk-grab` 加進 pointerdown 那串排除選擇器），
    不然拖邊會變成拖畫面 —— 跟 ⋮⋮ 和刊頭同一個坑。
    ⚠️ **摺線現在推得動了**（2026-08 改）。以前 `fold:true` 的容器整條擋掉
    （「紙對折的地方，不是誰切出來的一刀」），可是多數版面就是「一刀對折、
    每半邊往下切」—— 那表示**每一塊版位的寬都改不了**，而寬正是最常要調的那個數字。
    推得動之後兩半的比例會變，摺線跟著挪（印出來就是不對稱的對折），
    整張紙的大小照舊不動。摺線還是**畫**不出 resizer（`.fold` 不可拖），
    要改只有這兩條路：欄位打字、或拖版位的邊。
  - **`sh.mastheadW` 現在真的是版位的寬度**（`syncSheetCm()`，掛在 `openPlan()` 和
    `rebuildAll()` 上）。以前它是建版位時寫死的 `CONFIG.defaultMasthead.w`＝36.6，
    **從來沒跟著切出來的形狀走** —— 於是每一塊版位在 落版 都自稱 36.6cm 寬，
    版位高（`mastheadW / aspect`）和刊頭那一欄的公分跟著全錯。
    px 換 cm 只是一個比例（版子本來就照真實公分比畫，見 `boardPx()`／`blockCm()`）。
    ⚠️ 舊檔期第一次開起來要把**刊頭高度等比換算**並蓋 `plan.cmV2` 這個章當場存檔
    （照 `plan.guidesCm` 那條先例）：那些高度是照假的 36.6 訂出來的，直接留著會讓
    別人排好的刊頭當場變高或變矮。寬和高同乘一個比例，刊頭佔版位的比例就一步都不動。
  - **列數打得進去**（`addRowCountField()`）：**格數不動，只是重新分** ——
    40 格從 4 列 ×10 改成 5 列 ×8。寫進 `sh.rows`（跟拖 ⋮⋮ 搬列同一個欄位：
    那個欄位的意思本來就是「這個人要的排法」），清空＝放掉，回去跟著版位的形狀自己排。
    ⚠️ 真的排成幾列是 `renderGrid()` 算完才知道的（列數從版位的形狀推出來），
    所以那個欄位的數字由 `syncRowCountField(m)` 在 `renderGrid()` **兩條路**各回填一次；
    只在 `renderHead()` 裡填會永遠慢一步。焦點在裡面時不回填（人正在打字）。
    ⚠️ 只在 落版：列怎麼排是這一頁談好的事（跟 ⋮⋮ 只在 落版 同一條理由）。
- **一列旁邊的 ＋／− 會改格數**（`changeRowCells()`，落版和拼板都有、兩個身分都能按）。它同時動三個地方：`sheet.cells`、那一格所屬品類的 `groups[gi].count`、以及釘住每列幾格的 `sheet.rows`。三個沒一起動就會對不起來（總體的數字、版面的列、格子本人）。品類減到 0 要整條拿掉並把後面的 `gi` 往前挪。
- **身分在登入頁決定**（`login()` → `activeMode` ＝ `currentUser.role`）。假登入，沒有密碼：設計／PM／行銷各三位（`USERS`，存 `board_users_v1`，登入頁上可以加人）。**進去之後撥不動** —— 身分列上那三顆和換 PM 的下拉都拿掉了，要換人就右上角「登出」（清掉 `board_user_at_v1` 再整支重載：半路抽換身分要重畫的東西太多，漏一個就是看不出來的錯）。記住上次那位，下次直接進去。
  預設九位是「設計 A／B／C」「PM A／B／C」「行銷 A／B／C」（`seedName()`）。
  舊的天干名字（設計甲…）開機時會改成字母版（`renameSeedUsers()`）——**只改名字，id 一個都不動**，
  而且只改還叫著舊預設名的那幾位，自己改過名／自己加的人不碰。
  **PM 固定就是三位**（`keepThreePms()`）：以前 `ensurePmPerCell()` 會照版位數自動補人
  （PM 丁、戊、己…），登入頁那一欄會愈來愈長，而那些人從來沒有真的存在過。
  開機時先確保 A／B／C 在，再把其他 role 是 pm 的收掉。設計和行銷不修剪 ——
  那兩邊沒有自動長人這回事，加進去的都是有人刻意加的。
  **便利貼不受影響**：它記的是貼的當下的 `whoId` 和名字，不是每次去名單裡查，
  所以舊留言照樣顯示「PM 丁」，只是沒有人能再用那個身分登入。
  加人的時候**id 絕不重用**：便利貼記的是 `whoId`，換了 id 舊留言就變成別人的。舊的 `board_pms_v1` 搬進 `USERS` 時 id 也是原封不動。
  網址可以指定：`?as=design|pm|mkt`（那個身分的第一位）或 `?user=<id>`。**測試一定要帶** —— 每次都是乾淨的瀏覽器，不帶就停在登入頁。
  （反過來說：**帶著 `?as=` 就測不到登出**，重載之後它會自己登回去。測登出要用不帶旁路的網址。）
  名牌和「登出」是**同一顆 DOM 搬來搬去**（`hostWho()`，跟小地圖的 `hostSheet()` 同一個做法）：
  檔期列表、編輯器的工具列、開檔表的標題列，三條列都掛得到它。每一條各放一份的話遲早
  有一份忘記跟著更新，而它寫的是「你是誰」—— 那是最不能寫錯的一件事。
  它是 `#whoBadge`（id 不是 class）：`.who` 已經是便利貼卡片上「誰貼的」那一個。
- **三個身分**（`activeMode`）：設計、PM、行銷。行銷看得到三個分頁、改得動 落版，但 **拼板 只能看**：`isMkt()` 擋掉開格子，畫布收到的身分是 `'pm'`（零件搬不動）。他不在那串關裡（`myTurn()` 對他永遠是 false），所以橫幅上不會寫「輪到你」。
  **拼板 左邊那條（`#typeWrap` ＝字級＋工具）只有設計看得到**（2026-08）：字級是排版，
  PM 只挑貨，行銷在這一頁只能看。⚠️ 收的只有 拼板 那一條 —— **落版 的 `#toolRail`
  是另一顆位置**（自己站一條），行銷改得動 落版，那邊的 搬移／選零件／拖畫面 他真的在用。
  （以前這裡寫「工具列不因身分收起來」，理由是少一半的按鈕會讓人以為程式壞了。
   那條講的是**同一頁裡收掉一部分按鈕**；整條在那一頁本來就不屬於你，是另一回事。）
  **刊頭圖只有設計放得了**（`buildMasthead()` 的 `canImg === (activeMode === 'design')`）：
  那是整本 DM 的主視覺，一張放錯的會跟著整本印出去，而它是排版的一部分 ——
  PM 管的是格子裡的貨，行銷管的是這本 DM 什麼時候做、切成幾塊。
  兩個人都看得到那塊刊頭（版長什麼樣他們要看得到），但點不開檔案、拖不進圖、也沒有 ✕，
  而且那句「拖曳圖片到這裡」對他們換成「刊頭圖由設計放」：寫著卻按不動比沒有更難懂。
- **草稿：`plan.submitted`。** **沒有這個欄位＝已經提交** —— 判斷寫成 `isDraft(p) => p.submitted === false`，
  所以在這之前建立的每一個檔期都不會突然變成草稿（那會是最嚇人的一種 regression）。
  ⚠️ **從 切版 按「回上一頁」回來的那一張表上，帶入 還在**（`syncMode()` 的
  `!!editingPlan && !backToEditor`）：那一段人還在決定版怎麼切，「照上一次那本抄」
  正是這時候會想起來的事，而唯一的入口就在那張表上。挑一份之後**按儲存就整份蓋掉**
  現在切到一半的版位和品類 —— 帶入問的就是「照上一次那本開」，留著現在這一半
  只會是兩本混在一起。改時程那條路照舊收起來（那一檔早就在跑了）。
  草稿只有行銷看得到（`visiblePlans()` 是**唯一**的出口：檔期列表、PM 左欄那份清單、`?plan=` 深連結都走它），
  卡片上掛一枚虛線的「草稿・還沒提交」。
  總體 **右下角**那顆大的「**提交給設計**」（`renderSubmitBtn()`）只有行銷＋草稿＋在 總體 才出現，
  它不站在工具列右邊那排小鈕裡（`#submitBtn.big`，`position:fixed`）——
  草稿那一段整頁只有這一件事要做。
  **左下角是它的另一半：「← 回上一頁」**（`#draftBackBtn`，跟提交同一個 `on` 條件）＝
  回開檔表改名稱／類別／標籤／時程。上一步在左、下一步在右，跟開檔表上那一列同一個方向。
  走的是 `PlanForm.open(plan, {back:'editor'})`：那個旗標讓表上的 **儲存** 和 **返回**
  都回到 總體（`leave()`），不是掉回檔期列表 —— 一顆寫著「回上一頁」的鈕按下去回不來，
  比沒有那顆更難懂。回去是整個 `openPlan()` 重開（名稱、日期一改，橫幅、進度、
  現在第幾關全部要跟著算，自己補幾行遲早漏一個），代價是那一檔的復原紀錄重來一次 ——
  跟從列表重開一次一樣，切好的版一刀都沒少。
  ⚠️ 從卡片上的「改時程」進去的那條路不帶那個旗標，照舊回檔期列表（那裡本來就沒有「上一頁」）。
  同一段裡**進度**、**匯出乾淨版**、**左欄**三樣都收起來：
  進度問的是「現在哪一關、挑好幾格」，匯出印的是拼好的版位，這時候兩個都還沒有；
  左欄是清單＋小地圖，而要盯著切的那張版就攤在右邊。
  ⚠️ 收左欄要在 `fitBoards()` **之前**（`VIEWS.total.mount()` 的順序），不然第一眼是照
  「有左欄」算出來的小版，下一次重排才自己長大。
  **每一塊版位都要有品類、每一項都要有支數**才按得下去；沒填完的自己框紅（`.empty-seg`，只在 `.drafting` 底下），
  鈕上直接寫「還有 1 個沒填品類、1 個沒填支數」。提交＝`submitted = true`，是「可以開始了」不是一道鎖 ——
  版面之後照樣改得動，也沒有「收回」。
  它掛在 `paintTotalStates()` 和 `rebuildAll()` 上：切一刀、改品類、Undo 都會經過那兩條，
  另外找地方掛遲早會漏掉一條路。
  **草稿那一段行銷只看得到 總體**（`tabAllowed()`／`syncTabs()`）：格子是照 `groups[].count`
  長出來的，所以還沒填支數之前的 落版／拼板 排的是還不存在的東西 —— 點進去是一片空版，
  看起來像自己剛切好的版沒有算數。提交那一下兩顆分頁就回來。
  哪幾個分頁看得到只由 `tabAllowed()` 決定，
  真的要換頁只在 `applyMode()` 和提交那兩條路上做 —— `syncTabs()` 被叫的地方很多，
  順手 setTab 會在人拖東西拖到一半換頁。
  **草稿是唯一的差別**：`tabAllowed()` 裡只剩那一條，**PM 三頁都看得到**
  （2026-08 改回來，以前只給 拼板）—— 少了 總體／落版，PM 就看不到「這一塊在整本
  DM 的哪裡」和「這一塊怎麼排」，而審稿、校對本來就要對著整本看。看得到不等於
  改得動：閘在動作那一層。復原／重做也跟著給（動的是 落版 那棵樹，他現在看得到）。
- **開檔表＋時間軸**（`PlanForm`，`#view-plan`）＝行銷開一個新檔期、或改一個檔期的時程。整段從行銷那一支（`落版單系統.html`，已併入並從版控拿掉）搬過來。
  「＋ 新增檔期」在檔期列表右上角、「改時程」在卡片上。
  新檔期建出來是**草稿**（`submitted:false`）＋一張**空白**的版（一面兩半，`未命名` 0 支），
  按「下一步：切版」直接進 總體 開始切 —— 不是 `buildLayout()` 那份四個版位的示範資料，
  真的開檔期時冒出「刮鬍刀 14 支」只會讓人以為程式記錯了。
  改時程走同一張表存回原檔期（不會多生一筆）。
  **它是一個 IIFE，只露出 `open(plan|null)`。** 兩支程式的頂層名字撞得很兇
  （`render`、`setMode`、`savePlans`、`saveState`、`initBoards`、`pxPerCm`、`DM_SIZE`…），
  而且**模組裡一行都不能在定義當下執行** —— `buildGrid()`／`render()` 一律延到第一次 `open()`。
  ⚠️ **搬 CSS 進來一樣要隔開**：整包收在 `#view-plan` 底下。`.bar`（工具列）、`.track`
  （磁吸對齊那顆開關）、`.row`（落版的列）這幾個名字這支程式本來就在用，
  沒隔開的話時間軸會繼承到 42px 寬的開關和工具列的左右內距，旗子全部擠在最左邊。
  撞名的**改用 id 選**（`#bar`、`#track`），不要靠權重硬壓過去。
- **帶入**（開檔表上那顆，`#impMask`，收在 `PlanForm` 裡面）＝照過去某一檔開：
  左邊用名稱／標籤／類別／總長篩，右邊挑一張，把類別、標籤、各階段天數、
  **版位切割和品類**一次帶進來。同一檔期年年在跑，版位怎麼切多半照抄上一次的 ——
  只帶天數、讓人對著上一次的版重切一遍，才是真的白工。
  只列**已經提交過**的（草稿是別人還在切的半成品，照它開沒有意義）。
  ⚠️ **只複製骨架**（`cloneSkeleton()`：`dir`／`size`／`fold`／`name`／`groups`）。
  整棵樹直接 clone 會把上一檔挑好的貨、便利貼、走到第幾關連同格子本身一起搬過來 ——
  那是別人一整季的工作，不是版型。`blk()` 會給全新的 id 和一份空的 sheet，那正是要的。
  （原本行銷那支存的是 HTML 字串，天然不帶這些；改成樹之後就會帶了。）
- **過往檔期／回收桶**（檔期列表右上角那兩顆）。已經出稿的不佔首頁，收進 📁 那個彈窗
  （每個人都看得到 —— 回頭翻上一季的 DM 是三個身分都會做的事）。
  **刪除是行銷的事**，卡片上那顆 🗑；刪掉是**搬**進回收桶，不是抹掉。
  ⚠️ **回收桶只在這台機器上**（`catalogue_trash_v1`）。刪的時候共用資料庫那一列是
  直接 `queueRemove` 拿掉的，別台機器重整就看不到了 —— 也就是說「還原」只還原得了
  你自己刪的那一份。這是刻意的：回收桶是給手滑用的，不是版本控制。
  還原＝寫回 `plans` ＋ `queueUpsert` 送上去。
  **確認框（`#modalHost`）的 z-index 是 700，比那幾張大視窗（600）高** ——
  它問的多半就是「你剛剛在那張視窗裡按的那一下」，開在後面會看得到一半、按不到，
  而且不像壞掉，像那顆鈕沒反應。
- **沒有「目前階段管理列」這回事了**（2026-08 拿掉）。檔期列表最上面本來有一條
  `#statusBar`：列出每一檔、點名字攤開、點階段就把時鐘撥到那一關（`jumpToStage()`），
  加上收起來的把手（`catalogue_statusbar_folded`）。整條連 CSS 一起沒了 ——
  它答的是「撥到哪一關」，而名牌旁邊那顆 🕒 答的是「撥到哪一天」，同一件事兩個入口，
  而那一條還佔著檔期列表最上面一整條。
  **手動指定某一檔在哪一關（`plan.stageOverride`）更早就拿掉了**：那一關決定的不只是
  一條列上的字，版面上「球在誰手上」也照著它走 —— 一個人按一下就把整本 DM 換手，
  那不是時程，是蓋章。現在走到第幾關**只由日期算出來**（`autoStageIndex()`），
  卡片和版面讀的是同一支 `currentStageOf()`，所以它們永遠不會各說各話。
- **檔期卡下半：這一檔挑到哪了 ＋ 拉得開的小地圖**（`appendCardMap()`，從行銷那一支搬回來）。
  收起來是一條方塊、一格一個版位（看得出還有幾塊沒做），拉開才是版面本人 ——
  形狀跟編輯器左邊那張小地圖同源（同一棵樹、`effSizes()` 同一套百分比），只是不能點。
  「挑完」＝**每一格都有貨**（`blockPicked()` 看 `cell.board.state.picked`），
  那正是 PM 在拼板要做的事。長寬比照**這一檔自己的** DM 類別算（`planCm(plan)`）——
  `boardCm()` 問的是「現在開著的那一檔」，列表上沒有那個東西。
  **哪幾張卡是拉開的記在 `mapOpen` 這個 Set，不是記在 DOM 上**：卡片每 30 秒被整批
  重建一次（倒數），記在 DOM 上的話你拉開的那張會自己合起來。
  `.mm-blk` 的三種狀態**只換底色和字色，邊線一律不碰** —— 邊線講的是「版面切在哪裡」，
  跟做到哪無關；蓋掉的話整張版做完之後那些黑線會消失，十塊綠色黏成一大片。
  （行銷那一版每一塊上還寫著「這塊歸哪位 PM」。一格一位 PM 拿掉之後沒有那件事了，
    改成寫「挑好幾格」。）
- **檔期列表那一行上的按鈕整排都是行銷的**（`syncListButtons()` 一個地方決定）：
  新增檔期、改時程、🗑 刪除、匯入、📁 過往檔期、🗑 回收桶、清空資料。
  講的都是「這本 DM 存不存在、什麼時候做」——那是排檔期的人的事；設計和 PM 手上永遠是
  「別人開好的檔」，他們的列表列上只剩標題和名牌。
  （「壓力測試」那顆不在這一排：它是 devtool，虛線邊、灰字，長得就不像正常流程裡的按鈕。）
  ⚠️ 這是**這一排按鈕**的規矩，不是權限系統 —— 版面本身照舊誰都改得動（見上面的身分那一條）。
- **整本 DM 的點陣圖**（`SNAPS`，2026-08 加）。兩個用途、同一批圖：
  **總體 上那顆「整本預覽」**（`#dmPrevBtn`／`setDmBitmap()`）和 **拼板 進場那張蓋布**
  （`showBoardCover()`：幾十支 iframe 一個一個冒出來的過程沒有人想看）。
  ⚠️ 那顆鈕是**開關，而且就地換掉** 總體 那兩張版（`.pv-over` 疊在 `.board-ws` 上，
  剛好同一個框）——不是浮層。浮層要人先開再關，而這是「同一張版的另一種看法」。
  尺規、標題、捲動都照舊，換掉的只有版面本身。切走那一頁要自己放下（`syncDmPrevBtn`）。
  ⚠️ 開著的時候**匯出乾淨版 匯出的就是它**（`exportDmBitmap()`）：匯出該匯出畫面上
  這一份，不該有第二種答案。印的是複製出來的 `#dmPrintHost`，不是 `#totalScroll` ——
  那裡面還坐著尺規（grid 的兩條軌道，`display:none` 之後整個 grid 會重排）、
  標題和小工具列。同時要把 `#board` 收掉：原本那幾條是無條件放行它的。
  **上層拍不到 iframe 裡面**（跨文件就是看不到），所以每一格是**自己**畫自己再送上來的：
  `2cell-product-pick.html` 的 `snapshotStage()` 把 `#stage` 整棵塞進 SVG 的
  `foreignObject`，讓瀏覽器畫到 canvas 上。不引套件（零相依）唯一畫得出點陣圖的路。
  ⚠️ **SVG 要用 `data:`，不可以 `blob:`**。blob 的來源跟著這一頁走，而這一頁常常是
  `file://` 直接打開的 —— 那時候它是 opaque origin，畫上去畫布就被污染，
  `toDataURL` 直接丟 SecurityError（第一版就是這樣整片空白的）。
  ⚠️ `foreignObject` 裡面**不載任何外部資源**：商品圖、logo 那幾個 `<img>` 要先一張一張
  轉成 `data:`（縮到 420px 再轉，不然一張原尺寸的照片就把那串撐到幾 MB）。
  Storage 回 `access-control-allow-origin:*`，所以 crossOrigin 之後不會污染畫布。
  ⚠️ **字型也算外部資源**（`snapFontCss`）：價格用的 Anton 是 `<link>` 拉進來的，
  而 SVG 被當成 `<img>` 畫的時候不會去載它 —— `font-family:Anton` 直接掉回 sans-serif，
  拍出來的價格**比螢幕上細、也比較窄**。所以要抓那支 woff2 轉成 data: 的 `@font-face`
  跟著 CSS 一起塞進去。只要基本拉丁那一個 subset（Anton 只用在價格和 SKU，都是數字）；
  一支 iframe 抓一次就留著；抓不到就回空字串（斷網、被擋、CORS）——
  那時候價格會是替代字，但總比整張圖拍不出來好。
  （`driver.mjs` 預設把 `fonts.*` 擋掉，所以測試裡這條路本來就是空的：
    要看真正的字型跑 `--fonts`。）
  ⚠️ **複製的是 `#stage` 整棵、外面包 `<body class="ui-stage">`**：那幾條 CSS 是
  `body.ui-stage #stage/#wrap/#grid` 一路串下來的，少一層就照「沒被嵌住」的樣子排。
  ⚠️ **刊頭也要進去**（`__mast`）：它是一塊版位裡最大的一片，少了它預覽上是一個大洞，
  而那個洞看起來像「這裡什麼都沒有」。但它**不必拍** —— 刊頭是上層自己的 `<img>`
  （不在 iframe 裡），手上就有網址，記下來給預覽貼同一張就好。
  **蓋布要跳過它**：上層那一張這一幀就畫得出來，蓋了反而會等不到 `ready`、
  一路掛到 6 秒的保險。刊頭被拿掉時要 `delete`，不然預覽上那一張不會消失。
  ⚠️ **幾何記兩份**（`captureBlockSnap`）：畫布那支 iframe 的框，和**格子自己的框**。
  畫布是縮好之後**置中**擺在格子的框線裡面的（見 `fitCellCanvases`：框線 ＋ 最多 4px
  的細邊），所以兩個差一圈，而那一圈：
  ・**蓋布用 iframe 的框** —— 它疊在真的版面上，差幾 px 就變成**每一個字都印了兩次**。
  ・**預覽用格子的框** —— 它底下什麼都沒有，照 iframe 的框貼會在每一格之間露出底下
    那張白紙，也就是「整本預覽上一格一格中間的白線」。圖裡本來就含那一格的底色
    （`stageSnapshot` 帶 `bg`），撐到整格就是滿的（`.pv-blk img` 是 `object-fit:fill`）。
  ⚠️ 幾何存的是**佔那一塊的百分比**不是 px（預覽畫得很小、蓋布是原尺寸），
  而且基準是 **`boardInkRect()`＝所有格子＋刊頭的聯集**，**不是 `#board` 的 rect**：
  每一列左邊還坐著一支拖曳把手（`.grip`，14＋6px，列印時本來就 `display:none`），
  拿 #board 量的話每一塊左邊都多一條 2.7% 的白邊，而且每塊寬度不同、白邊還不一樣寬。
  蓋布也要照這個框擺（不是 `inset:0`），不然每一格往右偏一點點，看起來像沒對準。
  ⚠️ **沒拍到的那幾格下一輪照樣要問**（`fresh && have.has(id)` 才跳過）：拍照的時候
  有的畫布還在開機，訊息處理器還沒掛上，那一問就掉了 —— 全部跳過的話那一格會空
  整整一分鐘（節流），而畫面上看不出它是「還沒拍到」還是「本來就沒東西」。
  ⚠️ **收在 IndexedDB**（`pm_snaps_v1`，2026-08 加），**不進 plan、不進 localStorage** ——
  圖不進 plan 是這個專案踩過的坑（base64 撐爆 localStorage、每次存檔重傳好幾 MB、
  歷史那 50 份快照各留一份）。而它也不該跟著同步給別人：那是「你上次看到的樣子」。
  ⚠️ **沒有這一層，整個蓋布等於沒有用**：在這之前 `SNAPS` 只活在記憶體，一重整就沒了，
  於是**每次重新打開這支程式、第一次進 拼板 都是「沒有圖可以蓋」**，而那正是使用者
  幾乎唯一會遇到的一次。量過：改之前重整後第一次進場 `cover=0`，改之後 `cover=20`。
  （`demo/bitmap-cover.html` 上「第一趟沒得蓋、第二趟完美」講的就是這件事 ——
   真程式在這之前永遠停在第一趟。）
  ・`loadSnaps()` 在 `openPlan()` 最後撈這一檔的回來（不 await，撈不到也不能擋住開檔）。
  ・`flushSnaps()` **一分鐘寫一次**（＋`pagehide`／切到背景時各一次）：它記的是
    上次看到的樣子，不是資料，晚一分鐘沒有人會少東西。
  ・⚠️ **拿來蓋之前要比對簽名**（`snapSig()` ＝格子 id 串＋`mhRev`）：格子換過了
    （加減格、改品類、換刊頭圖）就丟掉不用 —— 對不上的蓋布會變成「兩份錯開的
    版面疊在一起」，比不蓋還糟。只認結構不認內容：內容變了那張圖只是舊，不是錯。
  ・⚠️ **兩週沒更新過的整筆丟掉**（`sweepSnaps()`，一個工作階段掃一次）：舊檔期的圖
    不會有人來清，一季一季累積下去就是一個只長不消的庫。
  ・⚠️ IndexedDB 開不起來（無痕、配額滿、某些 `file://` 設定）就整個功能安靜地關掉，
    退回原本「只活在記憶體」的行為 —— 不能讓它擋住程式起來。
  ⚠️ **版上那條路只拍得到你開過的版位**（沒開過的那一塊從來沒有 iframe），所以另外
  有一支**預熱**（`farmBlock`）在背景把沒去過的補起來 —— 不然整本預覽上十三塊裡
  十二塊寫著「還沒看過」，而那顆鈕問的正是「整本現在長什麼樣」。
  做法是畫面外一間**暗房**（`#snapFarm`）：一次一塊，照 `sheetMetrics` 排一份跟版上
  一模一樣的版（同樣的列、同樣的格寬，因為預覽貼的是佔那一塊的百分比），
  等每一格回報畫好了、拍完就整屋拆掉。拍照走的是**同一支** `captureInto()`。
  ・**只在 總體／落版 跑，切進 拼板 當場收工**（`farmStop()`）：那一頁本身就掛著幾十支
    畫布，再開一屋子等於把最重的一頁乘二；而預熱要遮的是「切過去那一下」，
    本來就發生在你還沒切過去的時候。開檔期、進 總體／落版、按下 整本預覽 都會排一次。
  ・⚠️ 暗房**不可以 `display:none`**（那樣裡面的畫布不排版，也就沒有東西可以拍），
    **也不可以推到畫面外**（看不到的 iframe 會被瀏覽器降速，一屋子一起降就等不完）。
    所以是留在原地、`opacity:0`、`pointer-events:none`、大小 0 而 `overflow:visible`。
  ・⚠️ 暗房裡的畫布掛 `cell=farm:<id>`：它們回報的東西**不可以**被當成「版上那一格」
    收下來（那會把預熱的結果寫進正在編輯的檔期）。前綴讓主訊息路自己認不出來，
    開機要給的那一包（身分、目錄、logo、底色、上次拼好的內容）在預熱那條路再給一次。
  ・⚠️ **版面的數字要拿上一次量得到的那個窗口**（`paneBox()`）：總體 這一頁的
    `#sheetPane` 停在別的分頁上、`clientWidth` 是 0，照它算會排出一份列數完全不同的
    版，而那一份會被當成「這一塊長這樣」貼上去。
  ・⚠️ **要印之前先拆**（`exportClean` → `farmStop()`）：暗房是 `position:fixed`，
    跟著印。而 `farmClear()` 只拆眼前那一份，正在跑的迴圈下一圈會再生一屋子 ——
    所以叫停是 `farmPaused`，要再開工一律經過 `farmSweep()`。
  ・⚠️ 只補**完全沒有圖**的版位，而且**挑起來的那一塊排第一**（`farmTodo()` 的 sort，
    `switchBlock()` 一挑就 `farmSweep(600)`）：挑了一塊，下一步多半就是切過去看它。
    ⚠️ 那一塊**曾經被跳過**，理由是「版上那份會自己拍」—— 那句話只有在 拼板 才成立，
    而暗房本來就只在 總體／落版 跑（那兩頁上那一塊一支畫布都沒有，沒有人會拍它）。
    跳過的下場是**唯一沒有蓋布的剛好就是你正要打開的那一塊**：在 總體 點一塊、
    切過去 —— 那正是最常走的路，於是整件事看起來像完全沒生效。
    ⚠️ 拍一塊要花的時間跟排一次差不多（一兩秒），所以**點完兩秒內就切過去還是來不及**。
    這是下限，不是 bug：那張圖是它自己排一遍畫出來的。
    拍不出東西的（一格都還沒挑貨）記一個時間，90 秒內不再試：那不是黑名單，
    是不要空轉 —— 過一陣子 PM 可能就挑了。
  ⚠️ 真的還沒有圖的那一塊，預覽上畫斜線寫**「還沒拍到」／「還沒挑貨」**（兩種空
  差的是一整塊版的工作），**不要留白** —— 留白跟「真的一格都沒挑」長得一模一樣。
  ⚠️ **沒挑貨的格子也要記一筆**（`url:''`，只有框和底色；還沒放圖的刊頭同理，
  `mast:true` 畫虛線框）。以前只記「有畫布的那幾格」，於是一塊挑一半的版位在預覽上
  是**幾張商品圖浮在一片白上** —— 看不出格子在哪、也看不出它有幾格，而「挑一半」
  正是這本 DM 大部分時間的樣子。facade 要嘛長得像真的，要嘛就別蓋。
  ・因此 `.size` 不再等於「有圖」：要問**真的有沒有圖**一律走 `snapHasArt()`
    （`farmTodo`、那顆鈕的計數都是）。拍照時「這一格有了沒」也要看 `.url`，
    看 `has(id)` 會把只有框的那幾格永遠跳過。
  ・沒有畫布＝這一格真的沒貨，所以那一筆**每次都覆蓋**（貨被拿掉的格子不能留著舊圖）；
    有畫布的先放一個框墊著，拍到了再換成圖。
  ・蓋布不蓋只有框的那幾筆：底下那一格本來就長那樣，而且它們永遠不會回報
    `stagePainted`，蓋了就只能等 6 秒的保險。
  ⚠️ **改過的那一格不受一分鐘節流管**（`snapStale`／`staleSnapCell()`，2026-08 加）。
  節流是為了「排版的人本來就一直在停」，但真的改過的那一格被擋住的話，下次進場蓋的
  就是一分鐘前的樣子 —— 而那張圖看起來跟真的一模一樣，人只會以為自己剛剛的改動沒存到。
  **改一格就只重拍一格**，不是整塊六十格重來。掛在四個真正會改變那一格長相的出口：
  `noteBoardChange()`（挑貨、搬零件、刪東西）、`applyPaint()`（底色是烤進圖裡的）、
  `pushType()`（字級一改整塊每一格都不一樣 → 走 `staleSnapBlock()`）、換刊頭圖。
  ⚠️ `pushType(block, find)` 帶 `find` 的那一路是**預熱**在拍別的版位，不算「使用者改了什麼」，
  不要標記。
  ⚠️ 拍照掛在 `paintBoardThumbs()` 上、手停 2.5 秒才拍，而且**一分鐘內拍過就不再拍**：
  排版的人本來就一直在停，每停一次重新光柵化幾十格會被感覺到。
  蓋布是**整張版一張圖**（一塊 canvas，`COVER_Q`＝0.42 的解析度畫、CSS 放大回原尺寸），
  **不是一格一張**（2026-08 改，做法照 `global-bitmap-cache.html`）。
  一格一張的毛病是它只蓋得到「上次拍到的那幾格」—— 中間挑過貨的、剛切出來的都是洞，
  而洞裡就是真的版面在一個一個長出來。整張畫就沒有洞：有圖的貼圖，**沒圖的由這一層
  自己畫框和底色**，每一格都被蓋住。
  ⚠️ **幾何一律現場量**（每一格當下的 rect），只有「圖」來自快照 —— 快照裡那份幾何
  可能是上一次的版面，拿它來擺就整片錯開。
  ⚠️ 蓋著的時候**真的那一層整個藏起來**（`#board.covered > .row/.masthead{opacity:0}`），
  不是「蓋在上面」而是「換掉」。列和刊頭各自淡，`#board` 本身不能動 —— 蓋布是它的
  小孩，一起淡掉就等於沒蓋。
  ⚠️ **解析度跟著螢幕的實際畫素走**（`coverQuality()`＝`devicePixelRatio`，最高 2）。
  本來刻意畫糊（0.42），理由是「它下一刻就要被換掉」—— 那個理由在「只有使用者動手
  才換成真的」之後就不成立了：它會一直蓋到人動手為止，糊的就看得出來糊。
  ・⚠️ 但要有**畫素預算**（`COVER_MAX_PX`＝4M）：蓋布是照「版面沒縮放時」的大小建的
    （進場那一趟 `zoomToFit` 還沒跑），一張全開的 DM 到四千多 px 寬，乘上 HiDPI 就是
    一塊上億畫素的畫布，而它顯示出來只有幾百 px 寬。
  ・⚠️ 真正的上限是**來源**：每一格那張 jpeg 是畫布自己拍的（2cell 的 `SNAP_SS`＝1.8 倍
    ＋ `SNAP_Q`＝.86），這裡再放大也長不出它沒有的細節。SVG 是向量的，畫到多大就在
    多大的尺寸上光柵化，所以品名和價格是**真的**變細緻；裡面那幾張商品照是點陣的，
    到它們自己的解析度為止。
  ・⚠️ `SNAP_SS`／`SNAP_Q` 直接乘進**記憶體和 IndexedDB**（一格一張 jpeg × 幾十格 ×
    十幾塊版位）—— 量過：1.8 倍 ＋ .86 之後一格約 5～6KB（原本約 2KB）。
  仍然**用交叉淡出**（240ms）：兩層之間永遠有清晰度差（來源就是點陣的），
  硬切看得出來。
  **什麼時候換成真的：只有一個條件 —— 使用者動手了**（滾輪、按下去、按鍵；
  `armCoverIntent()`）。在那之前他只是在看，而蓋布長得跟真的一樣，多蓋一秒沒有成本。
  ⚠️ 本來還有第二個條件「每一支新畫布都回報過了」，**拿掉了**：那件事要靠幾十支
  iframe 各自誠實回報，而那個訊號有太多種不準的方式（`tellPainted` 一支只報一次、
  舊那批的回報還在路上、圖等到上限就照報）—— 「蓋布提早不見」追到最後**每一次**
  都是那個訊號在騙人。「有沒有動手」則是上層自己就知道的事，不必問任何人。
  ⚠️ 12 秒的保險留著，但它**不是「載好了沒」的判斷**，只是「不要永遠蓋著一張照片」：
  版面底下會變（每 5 秒合併一次別人的改動），而蓋布看起來跟真的一模一樣。
  ⚠️ `stagePainted` 那一句還留著，但**蓋布不再聽它** —— 現在只有**預熱**要它
  （暗房要等每一格畫好才拍得到東西，見 `farmBlock`）。
  ⚠️ 等的是 `stagePainted`，**不是 `ready`**。`ready` 的意思只是「iframe 起來了、
  可以收東西了」，那時候格子裡一個字都還沒有 —— 照它掀的話蓋布讓開之後你看著的是
  一格空白，內容才跳出來，那正是蓋布要遮的東西。畫布在 `restoreBoard()` 收完
  （東西真的排回畫面上）之後才報這一句（見 2cell 的 `tellPainted`）。
  ⚠️ **而且要等圖**（`imagesSettled()`，2026-08 加）：**版面排好 ≠ 好了**。
  商品圖和 logo 是 Storage 上的網址，各自要走一趟網路，`restoreBoard` 收完的那一刻
  它們多半還在路上；而蓋布那張是**烤好的**（`snapshotStage` 拍之前把每一張 `<img>`
  都轉成 `data:`，不然 foreignObject 裡什麼外部資源都載不到），所以它永遠完整。
  只等排版就掀＝**在圖還沒到的時候讓開：剛剛看得到的商品照整片消失，再一張一張
  回來**，掀開之後比蓋著的時候還醜 —— 那就是「還是看得到東西一個一個冒出來」。
  上限 4 秒（讀不到的圖也算「到了」），收在上層那 6 秒保險前面。
  這件事的可執行證據在 `demo/bitmap-cover.html`（那一頁的「排好就掀／到齊才掀」
  就是這兩種行為，同一組資料即時切換）。
  ⚠️ **空的那一格也要報**（`boardRestore` 是 null 時 `restoreBoard` 直接 return）：
  不報的話那一格會被蓋到 6 秒的保險才掀開，而它根本沒有東西要等。
  ⚠️ 順便等字型，但**最多 600ms**：價格用的 Anton 是網路字型，它到齊之前那幾個
  數字在替代字的位置、之後會自己跳一次（`fonts.ready` 那一段會重排）。
  斷網時等下去就是一直蓋著一張過期的圖，那比跳一下糟。
  ⚠️ **蓋不蓋只在 `paintBoardThumbs()` 開頭判斷一次**，而且問的是
  **「等一下有沒有畫布要生」**（`canvasMissing()` ＝這一格有貨、身上卻沒有 `.ccanvas`），
  **不是「版面有沒有重建」**。會重生畫布的路全部經過 `paintBoardThumbs()`
  （`switchBlock`、`showBoardOverview`、`reflowBoard`、`rebuildAll`、`afterMerge`、
  合併、換刊頭圖、以及從 落版 切回來），所以判斷寫這一次就夠。
  ⚠️ **這裡踩過兩次，兩次都是「整張版當著你的面長出來」**：
  ・原本只有 `switchBlock`／`showBoardOverview` 記得蓋，另外五條沒蓋。
  ・改成看「有沒有重建」（`boardRebuilt` 旗標）之後更糟：畫布是離開 拼板 時被
    `clearBoardThumbs()` 拆掉的，而**卡片留著** —— 回到 拼板 時 `sheetSig` 沒變、
    `renderGrid()` 走「只改大小」的快路、沒有重建，可是那 60 支畫布全部要重生。
    於是 **落版 → 拼板 完全沒有蓋布**，而那是最常走的一條路。
  ⚠️ **一支都不用生就不准蓋**：畫布沒被換掉的話它們早就報過 `stagePainted` 了
  （`tellPainted` 一支只報一次），蓋上去沒有人會來掀，只能等保險 —— 蓋著一張
  過期的圖十幾秒，比不蓋還糟。
  ⚠️ **蓋的和等的都只能是「這一趟會生新畫布的那幾格」**（`canvasMissing` 算出來的
  集合傳給 `showBoardCover(waitFor)`）。放「所有有貨的格子」的下場是**提早掀開**：
  畫布還活著那幾格早就報過了、永遠不會再報，於是整張版吊到保險才一次掀 ——
  而那時候真的在重生的那幾格常常還沒好。切回來時有些畫布還在、有些被拆了，
  這種混著來的情況天天發生。
  ⚠️ **只認現在活著的那一支報的**（比 `contentWindow`）：進場那一趟版面會重建
  兩三次，被丟掉那一批的 `stagePainted` 可能還在路上（postMessage 是非同步的），
  照收就會把新那一批的等待數扣掉，蓋布在新畫布還一片空白時掀開。
  ⚠️ **驗這件事不能只斷言「蓋布不見了」**：`!document.getElementById('boardCover')`
  在「正常掀掉」和「從來沒蓋過」兩種情況都是 true —— 上面第二個坑就是被這個
  假陰性放過去的。要斷言的是**進場那一刻 `#boardCover img` 的張數**。
  ⚠️ **只蓋等一下真的會長出畫布的那幾格**（`pickedOf(c).length`）：貨被拿掉的那一格
  不會有畫布來報到，蓋了它就是整張版一起吊到保險時間。
  ⚠️ 保險是 **8 秒**，而且要**大於畫布自己那幾個上限的總和**（字型 600ms ∥ 圖 4000ms，
  加上幾十支 iframe 同時開機）—— 不然保險比畫布先到，那幾個上限等於白設，
  而搶先掀開看到的正是蓋布要遮的東西。以前是 6 秒。
  ⚠️ **蓋布要活過重建、而且每次重排都要重新對位**（`placeBoardCover()`，掛在
  `renderGrid()` 兩條路的出口）。切進 拼板 的那一趟裡版面還會再動兩次：意見欄站好之後
  `reflowBoard()` 重量一次，接著 `zoomToFit()` 換一個倍率。兩個坑都踩過：
  ・只在貼上去那一刻算一次 → 蓋布停在舊尺寸，螢幕上是**兩份錯開的版面疊在一起**
    （真的那份從底下透出來），比不蓋還糟。
  ・`renderGrid` 重建走 `boardEl.replaceChildren(frag)` → 蓋布當場被清掉，而**那一幀
    正好**是新卡片和幾十支還在開機的 iframe 冒出來的時候，等於它在最該蓋著的
    那一刻自己消失。所以要跟 `dropLine` 一樣接住再貼回去。
- **對方改了什麼，靠輪詢**（`pollRemote()`，每 5 秒；分頁切走就停，連不上就退避到 60 秒）。
  以前只有開機拉一次，之後除非重整永遠看不到別人的改動。
  問的是 `pullIndex()` —— 只撈 `id, updated_at, lastBy`（1～2 KB），有變的才 `pullSome()` 抓內容。
  ⚠️ **只比「一不一樣」，不比「誰比較新」**：`updated_at` 是用戶端自己寫上去的（見 `upsert`），
  兩台電腦時鐘差幾秒就排錯順序。`stampOf(id)` ＝「我相信伺服器上那一版是什麼時候的」，
  三個地方會更新它（整包拉、拉幾筆、自己上傳成功），**輪詢本身不更新** —— 探到的還沒吃進來。
  「是誰改的」不必改 schema：存檔時 `stampLastBy()` 把 `lastBy` 寫進 plan 自己的 JSON。
- **開著的那一檔絕對不被「換掉」**（換物件）。`currentPlan`／`layout`／`activeBlock` 都握著
  那個物件的參考，換掉手上那個就變孤兒 —— 之後每一筆編輯寫進孤兒裡，存的卻是新的那份，
  改了半天全部不見而且不報錯。要更新它只能**就地改欄位**，再 `rebuildAll(keepId)`。
- **「別人也在改」那條提示（`#remoteBar`）分三種，只有一種是琥珀色。**
  本來三種講同一句話、一律琥珀色、而且「重新載入」是藍色主按鈕 —— 看起來像警報，
  可是多數時候什麼事都沒有，而那顆最顯眼的鈕才是會丟東西的。
    ・**還沒動過手 → 根本不出現**：`adoptRemote()` 就地換成他那一份 ＋ `rebuildAll(keepId)`
      （比 `openPlan()` 溫柔，停在同一個版位上），配一句安靜的 toast。
      「打斷你，然後告訴你沒事」是最沒有意義的一種提示。正在拖東西時跳過這一輪。
    ・**動過手、不同版位 → 平靜地講一句**：會自動合併，你不必做什麼。
    ・**動過手、同一個版位 → 琥珀色**，而且講得出是哪一塊 —— 靠心跳裡的 `blockId`
      （`whoOnBlock()`）。那個資料本來就有，拿來把含糊的一句換成具體的一句。
  按鈕是「看他那一份」和「知道了」，**主按鈕給後者** —— 會丟東西的那一顆不該長得像建議。
  ⚠️ **「變了」不是只比時間戳，還要問「最後那一筆是不是我寫的」**（`lastById !== 我`）。
  只比時間戳會誤判自己：輪詢的請求和自己的存檔會交錯 —— `pullIndex` 在寫入前送出、
  寫入後才回來，帶回舊的時間戳，跟剛更新的 `stampOf` 一比就「不一樣」。
  **一個人用的時候那條提示就是這樣冒出來的。**
  ⚠️ 「知道了」＝`remoteMuted`，**這一檔就不再講了**。對方還在工作的話他每存一次檔
  就是一個新時間戳，不閉嘴的話這條會每 5 秒再跳一次 —— 比不講還煩。
  只有升級成「同一塊」才重新開口，換檔期也會重新開始算。
  ⚠️ 「他跟我在不在同一塊」是從心跳算的，所以 `paintPresence()` 最後要再叫一次
  `renderRemoteBar()` —— `pullBeats()` 不是 await 的，只在 `remoteHit` 變動時重畫的話
  用到的是上一輪的舊心跳，對方走進你這一塊時不會升級。
  ⚠️ `adoptRemote()` **只有骨架真的變了才 `rebuildAll()`**：對方在拼板挑貨的話版面形狀
  根本沒動，每 5 秒整棵重建一次會閃、而且很重。
  提示是**告知**，不是門：擋不擋得住由下面那條合併負責（以前這裡會停止上傳，
  有了三方合併之後不必了 —— 擋是「我不知道該怎麼辦」，合併是「我知道」）。
- **寫入是「條件寫入＋三方合併」**（`PlanSync.writeOne()` ＋ `mergePlanInto()`）。
  一份 plan 裡住著四種東西：檔期資料（行銷）、版面骨架（設計）、每個版位的列和刊頭（設計）、
  每一格的內容（PM＋設計，資料量的大宗）。**設計搬列的同時 PM 挑貨，改的是不同的東西，
  只是剛好住在同一列裡** —— 所以要修的不是「把衝突變細」，是別再把不相干的東西一起覆蓋。
  流程：**先樂觀 CAS**（`PATCH ?id=eq.X&updated_at=eq.<我以為的那一版>`，`Prefer: return=representation`；
  回空陣列＝有人插隊）→ 插隊了才把遠端讀回來三方合併 → 再 CAS，最多三次。
  沒人插隊時只有一個 PATCH，**不必先讀**（整包 143 KB，能不讀就不讀）。
  合併規則：我沒動他動了→用他的；我動了他沒動→用我的；**兩邊都動了→用我的並且講出來**（toast）。
  ⚠️ `mergePlanInto()` **就地改 mine**，不回傳新物件 —— `currentPlan`／`layout`／`activeBlock`
  都握著那個物件的參考，換成新的就全變孤兒。骨架換掉之後叫 `rebuildAll()`（Ctrl+Z 走同一條）。
  ⚠️ `lastBy`／`lastById` 刻意不參與比對：那是記帳欄位，每次合併兩邊都不一樣，
  列進去就每次都報一個假衝突，把真的那一個淹掉。
  ⚠️ 一格裡面**只有便利貼往下合**（`mergeNotes()`）。挑了哪些貨、零件擺哪、走到第幾關
  是一整組互相有關係的東西，各取一半只會做出一個誰都沒排過的版 —— 那些一律用我的。
  **便利貼是例外，而且是最常撞到的那一種**：PM 在某一格貼了一則問題，同時設計在那一格
  搬零件；整格用我的話那句話就沒了，而它是一句話，不是版面的一部分。
  便利貼 id 全域唯一（`'n'+Date.now()+亂數`），照 id 取聯集；回覆取比較長的那一串。
  刪掉的規則是**只有我撕掉的才算撕掉** —— 撕錯了再撕一次很便宜，話不見了要不回來。
  只有便利貼不一樣的話**不算衝突**（那一格已經整個合好了，沒有東西被丟掉）。
  ⚠️ 同步層把 report 交給上層時要**逐欄搬**：漏掉 `notes` 只是少一句提示，
  漏掉 `rebuilt` 是畫面不會重建 —— 骨架已經換成他的了，螢幕上還是舊的那一棵。
- **商品目錄也走同一套**（`writeCatalogue()` ＋ `mergeCatalogueInto()`）。它比檔期更該合 ——
  **全公司只有這一列**，兩個人各自貼一批商品進來，後存的把先存的整批抹掉。
  一列一個 SKU，SKU 就是那個商品的身分，所以照 SKU 取聯集；
  `model`／`brand`／`bimg`／`img` 是「商品名 → 值」的對照表，一把鑰匙一把鑰匙合。
  刪掉的規則跟便利貼一樣：**只有我刪的才算刪**（多一個商品在挑貨清單裡沒什麼，
  一整批不見要重貼一次）。
  ⚠️ 合併時**不可以用 `pullCatalogue()` 去讀對方那一份** —— 它會順手把 `catBase`
  換成剛讀到的，而那正是「對方」。祖先一旦變成對方，他新加的東西就會被當成
  「祖先本來就有、我刪掉了」而丟掉。要讀得乾淨（`readCatalogueRow()`）。
  同一個坑在 plans 那邊是 `pullSome()` 會記 stamp —— 讀取函式帶副作用，兩次都咬到。
- **誰在看哪裡**（`beat()`／`pullBeats()`）：一人一列 `__presence:<userId>`，**借 plans 那張表**
  （目前所有東西都只靠 anon key 打 REST，要 create table 就多一個未必拿得到的權限）。
  一個人只寫自己那一列 → 心跳之間永遠不會互相覆蓋。15 秒一拍，45 秒沒訊號就當作走了。
  ⚠️ plans 裡因此混進了不是檔期的列，**所有讀的路徑都要過 `isPlanRow()`**，漏一個就會在
  檔期列表上冒出一張壞掉的卡。
  ⚠️ 比對「誰在這一塊」**一定要連 planId 一起比**：版位 id 是每次開檔期從 1 開始編的（`b1`…），
  只比 blockId 的話，別人在另一檔的 b1 會在你這一檔的 b1 上亮起來。
  它擋不住任何事 —— 它做的是讓碰撞在發生**之前**被避開，那比任何合併演算法都便宜。
- ⚠️ **切到 拼板 之後那一幀的重量要叫 `reflowBoard()`，不能直接叫 `renderGrid()`。**
  多數時候那一趟走「只改大小」的快路（卡片和畫布都活著），但寬度變得夠多的時候
  `sheetSig` 會不一樣，那就是整批重建 —— 卡片重生、裡面的畫布跟著沒了，
  而 `renderGrid` 自己不會補畫。`reflowBoard()` 就是為這件事寫的（重建了就再
  `paintBoardThumbs()` 一次）。左欄換成 286px 那一疊之後這條路第一次真的走到重建，
  設計的拼板整片變成空格子，**而且完全不報錯**。
- 沒有存檔時預設是空的，要範例資料在網址加 `?seed=1`。「清空資料」在檔期列表右上角（行銷才看得到）。

## 寫程式的調子

- 註解用中文，寫**為什麼**，不寫這行在做什麼。既有的註解就是範例，跟著那個語氣寫。
- 位置只有四個就給四個落點讓人指，不要叫人用拖的去瞄準（刊頭換位置就是這樣改的）。
- 拖曳留給真正連續的量（高度、比例）。
- 動畫：版面重排時不要滑動 —— 列高、格數同時在變，滑動追不上，看起來像壞掉。

## git

`main` 是主線，工作開分支再發 PR。`gh` 在 `C:\Program Files\GitHub CLI\gh.exe`（可能不在 PATH 上）。
`backup/`、`turn/`、`*.bak*.html`、`out/` 都在 `.gitignore` 裡 —— 歷史本身就是備份。
