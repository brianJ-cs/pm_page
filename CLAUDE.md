# pm_page

DM 落版／拼板 編輯器。**單檔 HTML，沒有 npm、沒有相依套件、沒有打包** —— 用瀏覽器直接開就是完整的程式。請維持這件事。（`build-single.mjs` 叫 build，但它只是把檔案複製進 `public/`；同步層也是手寫 `fetch`，不引 CDN 函式庫。）

| 檔案 | 做什麼 |
|---|---|
| `design_and_PM.html` | **整支程式**：登入、檔期列表、開檔表＋時間軸、總體／落版／拼板、意見欄。三個身分都從這裡進。所有程式碼在**同一個** `<script>` 裡 |
| `2cell-product-pick.html` | 商品版面本人。被主程式嵌兩次：`?ui=stage`（格子裡的畫布）、`?ui=panel`（挑貨面板），靠 `postMessage` 溝通 |
| `supabase-sync.js` ＋ `config.js` | 共用資料層。零相依，只用 `fetch` 打 PostgREST；沒有 build，設定就靠 `config.js` 注入 |
| `_redirects` | `/` ＝主程式；`/editor` 指回 `/`（行銷那一支併進來之前，編輯器住在那裡） |
| `GLOSSARY.md` | **動手前先看**。「block」在這個專案有三個意思（版位／格／零件），吵過很多次 |

## 改完一定要跑起來看

語法檢查**擋不住這裡最常見的錯**。整支程式是一個 `<script>`：在頂層呼叫一個會讀到下面才宣告的 `let` 的函式 → `ReferenceError` → 整支程式停在那一行 → 開起來整頁是死的。這個坑踩過一次，而且「語法沒問題」完全看不出來。

```bash
node .claude/skills/run-pm-page/driver.mjs --file "design_and_PM.html?seed=1&as=design" \
  --wait 700 --click ".plan-card .btn.open" --wait 800 \
  --click-text "拼板" --wait 1200 --shot out/check.png
```

`as=design`（也可以 `as=pm`／`as=mkt`／`user=<id>`）是登入的旁路 —— 測試每次都是乾淨的瀏覽器，不帶它會整批停在登入頁。

跑完會印 `ok — no console errors, no exceptions`，有錯就列出來並 exit 1。`--eval` 可以順便量 computed style —— 改顏色時就是這樣證明「什麼都沒變」的。

## 顏色

- **新顏色一律在 `:root` 取個名字**，不要往下面塞 hex。名字照「做什麼用」取（`--surface`、`--ink-dim`、`--accent-tint`、`--danger-ink`），不照「是什麼顏色」。
- **藍色＝「你正在點的東西」**：分頁、選取、放下去的位置。東西本身不准用藍。
- **便利貼的顏色＝誰貼的**（設計天藍 `--sticky-design`／PM 黃 `--sticky-pm`，登入頁那兩顆點讀同一個名字），
  **圖示＝哪一種**，角落那個字＝哪一位（「設計乙」→ 乙）。判身分看 `whoId` 的前綴（`design:d1`），
  **不可以比 `=== 'design'`** —— 簽名變成「設計乙」之後那樣寫會把設計的貼紙判成 PM 的，只會被當成「顏色怪怪的」。
- **版位的底色＝狀態**：灰＝還沒提明細、白＝設計手上、綠＝整塊都在 PM。
  小地圖和總體現在**共用同一套**（鎖定那條特例跟著鎖定一起拿掉了）。
- 深色那一段（`操作區調深色`）放在 stylesheet **最後面**，靠來源順序覆蓋。淺色的東西要自己寫 `color` —— 不寫就會繼承深色底那套淺灰，白底上直接看不見（這個 bug 出現過三次：`#blockHead`、`.plan-card`、落版的字）。

## 大小

| 說法 | 意思 |
|---|---|
| 固定大小 | 螢幕上永遠同一個 px：便利貼以外的標記、版位標題 |
| 跟著縮放 | 乘 `var(--z)`：格子裡的品名、便利貼（它指著版面上某一個點）、刊頭上的說明字 |
| 只縮不放 | `scale(min(1, var(--z)))`：只有紅色數字 |

縮放只是把畫面拉近看，**不會改變印出來的東西**。

刊頭上的說明字本來是固定大小，2026-07 改成跟著縮放：那行字要看起來像「印在版上」的，
縮小了看不清楚就自己放大 —— 跟看真正的印刷品一樣。

## 資料

**兩層：`localStorage` 是本機快取，Supabase 是共用的那一份。** 開機先用 `localStorage` 秒開，再拉一次遠端；存檔照舊寫 `localStorage`，另外 debounce 上傳（`PlanSync.queueUpsert`）。後端不在時 `PlanSync.ok()` 回 false，整支走純 `localStorage`。

> ⚠️ **用瀏覽器直接開這幾支＝在正式資料上按按鈕。** 測試一律 `PM_NO_SYNC=1 node dev.mjs`，或用 `driver.mjs`／`scenario.mjs`（自己會擋）。

- **圖不進 plan，只存網址。** 商品圖、品牌圖、刊頭圖都走 Storage（`PlanSync.uploadImage`）。base64 塞進 `plan` 會把 `localStorage` 撐爆、每次存檔重傳好幾 MB，還被歷史那 50 份快照各留一份 —— 這三條都踩過。
  **Storage 的檔名＝那個東西的身分，不是使用者的檔名**（上傳是 upsert，同名就是同一個物件）：商品圖用品名、刊頭用 `masthead-<檔期>-<版位>`。用使用者的檔名，兩個版位各放一張 `banner.png` 就會蓋成同一張。
  **換圖要能看得到**：`sheetSig` 只記「有圖／沒圖」，換第二張簽名不變就不重建那個 `<img>` —— 所以換圖時 `sheet.mhRev` 加一，簽名讀它（不讀網址：base64 好幾 MB）。網址失效的圖會自己換成一句「讀不到」，不是一個破圖示。
- `catalogue_plans_v1` —— 整包檔期。版位、格子、便利貼（`cell.notes`）、拼好的畫布（`cell.board.state`）全都在 `plan.layout` 裡面。
- 因為都在 `layout` 底下，`snapshot()` 一序列化就全部有了 —— 存檔和 Ctrl+Z 是免費的。**改了資料要進歷史就叫 `commitAction()`，只想存檔叫 `markDirty()`。**
- **一格一顆棒子，兩邊來回傳**（`cell.st` ＝ 走到第幾關，0..7，沒有就是第 0 關）。關就是實際的流程，一關一個名字：提明細作業(商品部PM) → 製作(設計) → 一校(PM) → 一改(設計) → 二校(PM) → 二改(設計) → 蓋章(PM&商品部各主管) → 蓋章修改(設計) → 蓋章 → …（最後兩關來回，見 `CELL_STAGES`）。**「在誰手上」不另外存**，是這一關的 `side` 算出來的（`cellAt()`）—— 同一個問題不留兩個答案。蓋章＝「我這一關做完了，換你」＝走到下一關，兩個身分都能蓋。拼板 右邊貼紙旁邊那顆開蓋章，**點一格＝進下一關、拖一個框＝一次一片、Alt＝退回上一關**（方向看 Alt，不看格子在誰手上 —— 停在第一關的格照那條舊規矩會誰都推不動）。跟檔期列表那串 `STAGES`（提品／製作／…／出稿）是兩件事：那是整個檔期的排程日期，這是一格一格各走各的。
- **不是你的回合的格是壓暗，不是消失**：排版是整張版的事，看不到另外半張就排不了；棒子傳出去之後你也還得看得到它變成什麼樣。擋的只剩「往上面貼新貼紙」——「點進去」不擋（「送給 PM」會一次把整塊推過去，擋著的話設計送完就進不去自己的版）。便利貼是對話。蓋章模式裡對方那幾格也點得到（蓋錯了要拿得回來）。
- 沒有單調遞增的「進度」，只有**球在哪**：橫幅寫「在 PM n・在設計 m 格」。版位狀態 `blockState()`：全在 PM＝綠、一半＝黃色的「在 PM 8/14 格」、都在設計＝白、每一格都還在第一關（或還沒長出格子）＝灰的「待提明細」—— 它技術上都在 PM，但綠是「交出去了」，剛開的檔期整片綠看起來像做完了。（灰以前還兼著「未指派」，那件事沒有了，所以灰只剩這一個意思。）在**總體**上綠色只給整塊都在 PM 的 —— 顏色只有三級，講不出「一半」。小地圖跟總體同一套。
- 落版 角落那顆「**送給 PM**」＝把這個版位**還在設計手上**的格一次往前推一關（推一關、不是設成某一關：一改跟二改是不同的關，硬塞同一個值會把走到哪裡洗掉）。它是一個動作不是一道門：按幾次都行、沒有相對的「收回」（要拿回某一格就去拼板蓋章退回它）。
  **沒有「鎖定」這回事了**（`sheet.confirmed` 整條拿掉）：列永遠搬得動、刊頭永遠拉得動、搬移工具永遠可以用。以前那顆鈕一次管交件和「別再搬列了」，想交件就得接受版面被凍住。
- **沒有「這一格找誰」這回事**（2026-08 拿掉）：三位 PM 對整本 DM 一視同仁，每個人都看得到、
  點得進去、改得動。會壓暗的只剩「不是這一關」（`.notmine`）—— 那是流程，不是身分。
  以前是一格一位 PM（`cell.pm` ＋ 拼板／落版 的「指派」模式 ＋ 格子右上角那個「・甲」
  ＋ 小地圖藍點 ＋ PM 左欄的「我的品類」），整套已經連根拿掉。
  **舊檔期上的 `cell.pm`／`sheet.pm` 沒有去刪**：留在資料裡沒人讀就好，刪一遍要把每個檔期
  整包改寫上傳一次，換來的只是少幾個 byte。
- **總體 有自己的小工具列**（`#totalRail`，設計和行銷都看得到；PM 沒有這一頁）：**選擇（預設）／品類／切分／移動**，
  照行銷那一支的 點選／切割／互換 同一組圖示和規矩 —— 兩邊來回的人不必記兩套。
  預設那個工具**什麼都不改**：這一頁動的是整本 DM 的骨架，拿起 切分／移動 要是一個刻意的動作。
  **切分整套照行銷開檔那支搬過來**（原本在 `落版單系統.html`，已併入）：指到哪一塊就框紅（`cutAim`）、
  紅線寫著離那一塊的邊幾公分、滾輪切直／橫、最上面那條「切在 __ cm 處｜切下去」（填了就照數字切，
  空著照點下去的位置）。游標也跟著換：寫著「直」／「橫」的那兩顆 SVG（`--cursor-v`／`--cursor-h`）。
  內容留在第一半、第二半是空的（`未命名` 0 支）；
  移動＝拖到另一塊，換的是**內容**（品類、格子、刊頭），大小和比例不動（版位的比例是落版談好的）。
  兩個都走 `commitAction()` → `rebuildAll()`，所以 Ctrl+Z 退得回去。拖切割線調比例不歸工具管 —— 連續的量哪個工具都該拖得動。
  它是 `#view-total` 的第一個小孩，所以重建版面只清 `.board`（`clearBoards()`，以前是 `innerHTML=''`，一重建工具列就沒了）。
  **✏ 品類**（`#itemPop`）＝點一塊，填它要放哪些品類、各幾支，就是開檔那支「點選」做的事。
  支數是格子的來源（`buildCells` 照 `groups[].count` 生格），所以**改支數＝加減格子**：
  加的接在那個品類最後一格後面、減的從最後一格拿掉（`setGroupCount()`），**已經拼好的那幾格不動** ——
  不要重生一份 cells，那會把挑好的貨、便利貼、走到第幾關全洗掉。改完 `sh.rows` 要清成 null
  （釘住的列數對不上新的格數），版位的 `name` 也要跟著品類走（落版 的標題讀的是它）。
  最後一個品類被拿掉就留一塊「未命名 0 支」的位子 —— 整塊空白看不出是「還沒填」還是「畫壞了」。
  沒有這一顆之前，設計切出一塊新版位就得回行銷那一支才補得上內容。
- **總體 的單位是公分，不是「尺」**（2026-08 改）。`DM_SIZE` 照 `plan.type` 查這張版真正幾公分
  （全省對開大DM＝78.7×54.5），尺規一格一公分、寫數字的那幾條長一點，游標讀數、對齊線、
  切分的長度、切割線上打的數字全都是 cm。（以前開檔那支有自己的一份 `DM_SIZE`，改一邊要改兩邊。合併之後只剩這一份了。）
  以前是 `CONFIG.lengthDivisions/sideDivisions`＝20×10 格的「尺」，那個尺既不是台尺也不是公分，
  而版上其他東西（刊頭、列高）本來就講公分 —— 同一頁上不該有兩把尺。
  舊檔期的對齊線存的是舊格數，第一次開起來會換算並蓋 `plan.guidesCm` 這個章**當場存檔**
  （不存的話，開了沒改就離開，下次會再乘一次，線就跑到版外面）。
  **版子畫出來的比例也照公分算**（`boardPx()`：寬固定 742，高＝742×h/w）。以前寫死 742×525＝1.413，
  真正的對開是 1.444，差 2%：橫的一公分和直的一公分在螢幕上不一樣長，`blockAspect()` 量出來的
  「這個版位幾公分高」也跟著偏。小地圖那張紙同樣照這個比例（`miniPageH()`）—— 它是同一張版的縮圖，
  比例不一樣的話同一個版位在兩邊會長得不一樣胖。量長度還是分軸（`pxPerCm(el, vertical)`），
  那是保險，不是因為現在兩軸不一樣。
- **拼板 左邊那一欄：設計是小地圖本人，PM 是一疊兩段**（`#pmStack`：檔期 → 整本落版）。
  小地圖（`#sheetSide`）永遠是**同一顆** DOM 搬來搬去（`hostSheet()`），PM 這邊掛進第二段裡 ——
  兩邊同一份 `renderMinimap()`，不養第二套。段標題點一下收起來（記在 `pm_side_secs_v1`），
  外緣那根書籤收的是整欄。
  第一段就是首頁那份檔期清單的窄版（`renderPmPlans()`）：**換一檔不必退回列表**（`switchPlan()` → `openPlan()`，
  寫入還卡著會先問）。右邊那行寫「還沒落版」或「n 格」。
  中間本來還有一段「我的品類」，跟著一格一位 PM 一起拿掉了 —— 沒有那件事之後它只會列出
  整本 DM 的每一個品類，跟第二段那張小地圖講同一件事。
- **一列旁邊的 ＋／− 會改格數**（`changeRowCells()`，落版和拼板都有、兩個身分都能按）。它同時動三個地方：`sheet.cells`、那一格所屬品類的 `groups[gi].count`、以及釘住每列幾格的 `sheet.rows`。三個沒一起動就會對不起來（總體的數字、版面的列、格子本人）。品類減到 0 要整條拿掉並把後面的 `gi` 往前挪。
- **身分在登入頁決定**（`login()` → `activeMode` ＝ `currentUser.role`）。假登入，沒有密碼：設計／PM／行銷各三位（`USERS`，存 `board_users_v1`，登入頁上可以加人）。**進去之後撥不動** —— 身分列上那三顆和換 PM 的下拉都拿掉了，要換人就右上角「登出」（清掉 `board_user_at_v1` 再整支重載：半路抽換身分要重畫的東西太多，漏一個就是看不出來的錯）。記住上次那位，下次直接進去。
  預設九位是「設計 A／B／C」「PM A／B／C」「行銷 A／B／C」（`seedName()`）。
  舊的天干名字（設計甲…）開機時會改成字母版（`renameSeedUsers()`）——**只改名字，id 一個都不動**，
  而且只改還叫著舊預設名的那幾位，自己改過名／自己加的人不碰。
  加人的時候**id 絕不重用**：便利貼記的是 `whoId`，換了 id 舊留言就變成別人的。舊的 `board_pms_v1` 搬進 `USERS` 時 id 也是原封不動。
  網址可以指定：`?as=design|pm|mkt`（那個身分的第一位）或 `?user=<id>`。**測試一定要帶** —— 每次都是乾淨的瀏覽器，不帶就停在登入頁。
  （反過來說：**帶著 `?as=` 就測不到登出**，重載之後它會自己登回去。測登出要用不帶旁路的網址。）
  名牌和「登出」是**同一顆 DOM 搬來搬去**（`hostWho()`，跟小地圖的 `hostSheet()` 同一個做法）：
  檔期列表、編輯器的工具列、開檔表的標題列，三條列都掛得到它。每一條各放一份的話遲早
  有一份忘記跟著更新，而它寫的是「你是誰」—— 那是最不能寫錯的一件事。
  它是 `#whoBadge`（id 不是 class）：`.who` 已經是便利貼卡片上「誰貼的」那一個。
- **三個身分**（`activeMode`）：設計、PM、行銷。行銷看得到三個分頁、改得動 落版，但 **拼板 只能看**：`isMkt()` 擋掉開格子，畫布收到的身分是 `'pm'`（零件搬不動），**工具列不因身分收起來**（收過一版又收回：少一半的按鈕會讓人以為程式壞了，而「這一下做不做得到」本來就擋在動作那一層）。他不在那串關裡，所以不壓暗、也沒有「送給 PM」。
- **草稿：`plan.submitted`。** **沒有這個欄位＝已經提交** —— 判斷寫成 `isDraft(p) => p.submitted === false`，
  所以在這之前建立的每一個檔期都不會突然變成草稿（那會是最嚇人的一種 regression）。
  草稿只有行銷看得到（`visiblePlans()` 是**唯一**的出口：檔期列表、PM 左欄那份清單、`?plan=` 深連結都走它），
  卡片上掛一枚虛線的「草稿・還沒提交」。
  總體 右上角那顆「**提交給設計**」（`renderSubmitBtn()`）只有行銷＋草稿＋在 總體 才出現，
  **每一塊版位都要有品類、每一項都要有支數**才按得下去；沒填完的自己框紅（`.empty-seg`，只在 `.drafting` 底下），
  鈕上直接寫「還有 1 個沒填品類、1 個沒填支數」。提交＝`submitted = true`，是「可以開始了」不是一道鎖 ——
  版面之後照樣改得動，也沒有「收回」。
  它掛在 `paintTotalStates()` 和 `rebuildAll()` 上：切一刀、改品類、Undo 都會經過那兩條，
  另外找地方掛遲早會漏掉一條路。
- **開檔表＋時間軸**（`PlanForm`，`#view-plan`）＝行銷開一個新檔期、或改一個檔期的時程。整段從行銷那一支（`落版單系統.html`，已併入並從版控拿掉）搬過來。
  「＋ 新增檔期」在檔期列表右上角、「改時程」在卡片上。
  新檔期建出來是**草稿**（`submitted:false`）＋一張**空白**的版（一面兩半，`未命名` 0 支），
  按「下一步：切版」直接進 總體 開始切 —— 不是 `buildLayout()` 那份四個版位的示範資料，
  真的開檔期時冒出「刮鬍刀 14 支」只會讓人以為程式記錯了。
  改時程走同一張表存回原檔期（不會多生一筆）。
  **它是一個 IIFE，只露出 `open(plan|null)`。** 兩支程式的頂層名字撞得很兇
  （`render`、`setMode`、`savePlans`、`saveState`、`initBoards`、`pxPerCm`、`DM_SIZE`…），
  而且**模組裡一行都不能在定義當下執行** —— `buildGrid()`／`render()` 一律延到第一次 `open()`。
  ⚠️ **搬 CSS 進來一樣要隔開**：整包收在 `#view-plan` 底下。`.bar`（工具列）、`.track`
  （磁吸對齊那顆開關）、`.row`（落版的列）這幾個名字這支程式本來就在用，
  沒隔開的話時間軸會繼承到 42px 寬的開關和工具列的左右內距，旗子全部擠在最左邊。
  撞名的**改用 id 選**（`#bar`、`#track`），不要靠權重硬壓過去。
- **帶入**（開檔表上那顆，`#impMask`，收在 `PlanForm` 裡面）＝照過去某一檔開：
  左邊用名稱／標籤／類別／總長篩，右邊挑一張，把類別、標籤、各階段天數、
  **版位切割和品類**一次帶進來。同一檔期年年在跑，版位怎麼切多半照抄上一次的 ——
  只帶天數、讓人對著上一次的版重切一遍，才是真的白工。
  只列**已經提交過**的（草稿是別人還在切的半成品，照它開沒有意義）。
  ⚠️ **只複製骨架**（`cloneSkeleton()`：`dir`／`size`／`fold`／`name`／`groups`）。
  整棵樹直接 clone 會把上一檔挑好的貨、便利貼、走到第幾關連同格子本身一起搬過來 ——
  那是別人一整季的工作，不是版型。`blk()` 會給全新的 id 和一份空的 sheet，那正是要的。
  （原本行銷那支存的是 HTML 字串，天然不帶這些；改成樹之後就會帶了。）
- **過往檔期／回收桶**（檔期列表右上角那兩顆）。已經出稿的不佔首頁，收進 📁 那個彈窗
  （每個人都看得到 —— 回頭翻上一季的 DM 是三個身分都會做的事）。
  **刪除是行銷的事**，卡片上那顆 🗑；刪掉是**搬**進回收桶，不是抹掉。
  ⚠️ **回收桶只在這台機器上**（`catalogue_trash_v1`）。刪的時候共用資料庫那一列是
  直接 `queueRemove` 拿掉的，別台機器重整就看不到了 —— 也就是說「還原」只還原得了
  你自己刪的那一份。這是刻意的：回收桶是給手滑用的，不是版本控制。
  還原＝寫回 `plans` ＋ `queueUpsert` 送上去。
  **確認框（`#modalHost`）的 z-index 是 700，比那幾張大視窗（600）高** ——
  它問的多半就是「你剛剛在那張視窗裡按的那一下」，開在後面會看得到一半、按不到，
  而且不像壞掉，像那顆鈕沒反應。
- **目前階段管理列**（`#statusBar`，檔期列表最上面）：一個檔期現在走到哪一階段平常是
  **算出來的**（`autoStageIndex()`：第一個還沒過期限的那一關），但實際上會提前、會延、
  會卡住，而下游只看得到日期 —— 所以留一條手動指定的路（`plan.stageOverride`）。
  點檔期名字 → 點階段 → 確認。指定過的在卡片和管理列上都掛一枚「手動」，
  跟推算的不一樣時畫成虛線、滑過去看得到推算是哪一關。「回復自動」把 override 清掉。
  **卡片和管理列讀的是同一支 `currentStageOf()`**，所以它們永遠不會各說各話。
  它是開發／示範用的面板，收得起來（記在 `catalogue_statusbar_folded`）——
  收起來之後**把手還在**，不然收掉就再也開不回來。
  倒數每 30 秒只更新那幾個數字、不重畫整條：重畫的話你正攤開的那一檔會在你讀它的時候自己合起來。
  （行銷那一支的這一條裡還有「＋ 示範檔期」和「刪除檔期」。這支已經有「壓力測試」那顆，
    刪除也在卡片上 —— 同一件事不留兩個入口。）
- **檔期卡下半：這一檔挑到哪了 ＋ 拉得開的小地圖**（`appendCardMap()`，從行銷那一支搬回來）。
  收起來是一條方塊、一格一個版位（看得出還有幾塊沒做），拉開才是版面本人 ——
  形狀跟編輯器左邊那張小地圖同源（同一棵樹、`effSizes()` 同一套百分比），只是不能點。
  「挑完」＝**每一格都有貨**（`blockPicked()` 看 `cell.board.state.picked`），
  那正是 PM 在拼板要做的事。長寬比照**這一檔自己的** DM 類別算（`planCm(plan)`）——
  `boardCm()` 問的是「現在開著的那一檔」，列表上沒有那個東西。
  **哪幾張卡是拉開的記在 `mapOpen` 這個 Set，不是記在 DOM 上**：卡片每 30 秒被整批
  重建一次（倒數），記在 DOM 上的話你拉開的那張會自己合起來。
  `.mm-blk` 的三種狀態**只換底色和字色，邊線一律不碰** —— 邊線講的是「版面切在哪裡」，
  跟做到哪無關；蓋掉的話整張版做完之後那些黑線會消失，十塊綠色黏成一大片。
  （行銷那一版每一塊上還寫著「這塊歸哪位 PM」。一格一位 PM 拿掉之後沒有那件事了，
    改成寫「挑好幾格」。）
- **檔期列表那一行上的按鈕整排都是行銷的**（`syncListButtons()` 一個地方決定）：
  新增檔期、改時程、🗑 刪除、匯入、📁 過往檔期、🗑 回收桶、清空資料。
  講的都是「這本 DM 存不存在、什麼時候做」——那是排檔期的人的事；設計和 PM 手上永遠是
  「別人開好的檔」，他們的列表列上只剩標題和名牌。
  （「壓力測試」那顆不在這一排：它是 devtool，虛線邊、灰字，長得就不像正常流程裡的按鈕。）
  ⚠️ 這是**這一排按鈕**的規矩，不是權限系統 —— 版面本身照舊誰都改得動（見上面的身分那一條）。
- **對方改了什麼，靠輪詢**（`pollRemote()`，每 5 秒；分頁切走就停，連不上就退避到 60 秒）。
  以前只有開機拉一次，之後除非重整永遠看不到別人的改動。
  問的是 `pullIndex()` —— 只撈 `id, updated_at, lastBy`（1～2 KB），有變的才 `pullSome()` 抓內容。
  ⚠️ **只比「一不一樣」，不比「誰比較新」**：`updated_at` 是用戶端自己寫上去的（見 `upsert`），
  兩台電腦時鐘差幾秒就排錯順序。`stampOf(id)` ＝「我相信伺服器上那一版是什麼時候的」，
  三個地方會更新它（整包拉、拉幾筆、自己上傳成功），**輪詢本身不更新** —— 探到的還沒吃進來。
  「是誰改的」不必改 schema：存檔時 `stampLastBy()` 把 `lastBy` 寫進 plan 自己的 JSON。
- **開著的那一檔絕對不被輪詢換掉。** `currentPlan`／`layout`／`activeBlock` 都握著那個物件的參考，
  換掉手上那個就變孤兒 —— 之後每一筆編輯寫進孤兒裡，存的卻是新的那份，改了半天全部不見而且不報錯。
  所以改成掛一條 `#remoteBar`：沒動過手就只提醒；**動過手就停止上傳**（`remoteBlocked`），
  讓人選 重新載入（丟掉我的）／仍要覆蓋（丟掉他的）。擋住期間徽章寫「✓ 只存在本機」——
  寫「已儲存」會被讀成「共用資料庫上有了」，而那正是還沒發生的事。
  ⚠️ 擋住的時候 `savePending` 已經被本機那一半清掉了，所以要另外記 `uploadHeld`，
  解除之後直接 `queueUpsert()` 補送 —— 只叫 `flushSave()` 的話那一筆會安靜地消失。
  提示是**告知**，不是門：擋不擋得住由下面那條合併負責。
- **寫入是「條件寫入＋三方合併」**（`PlanSync.writeOne()` ＋ `mergePlanInto()`）。
  一份 plan 裡住著四種東西：檔期資料（行銷）、版面骨架（設計）、每個版位的列和刊頭（設計）、
  每一格的內容（PM＋設計，資料量的大宗）。**設計搬列的同時 PM 挑貨，改的是不同的東西，
  只是剛好住在同一列裡** —— 所以要修的不是「把衝突變細」，是別再把不相干的東西一起覆蓋。
  流程：**先樂觀 CAS**（`PATCH ?id=eq.X&updated_at=eq.<我以為的那一版>`，`Prefer: return=representation`；
  回空陣列＝有人插隊）→ 插隊了才把遠端讀回來三方合併 → 再 CAS，最多三次。
  沒人插隊時只有一個 PATCH，**不必先讀**（整包 143 KB，能不讀就不讀）。
  合併規則：我沒動他動了→用他的；我動了他沒動→用我的；**兩邊都動了→用我的並且講出來**（toast）。
  ⚠️ `mergePlanInto()` **就地改 mine**，不回傳新物件 —— `currentPlan`／`layout`／`activeBlock`
  都握著那個物件的參考，換成新的就全變孤兒。骨架換掉之後叫 `rebuildAll()`（Ctrl+Z 走同一條）。
  ⚠️ `lastBy`／`lastById` 刻意不參與比對：那是記帳欄位，每次合併兩邊都不一樣，
  列進去就每次都報一個假衝突，把真的那一個淹掉。
  ⚠️ 一格裡面**只有便利貼往下合**（`mergeNotes()`）。挑了哪些貨、零件擺哪、走到第幾關
  是一整組互相有關係的東西，各取一半只會做出一個誰都沒排過的版 —— 那些一律用我的。
  **便利貼是例外，而且是最常撞到的那一種**：PM 在某一格貼了一則問題，同時設計在那一格
  搬零件；整格用我的話那句話就沒了，而它是一句話，不是版面的一部分。
  便利貼 id 全域唯一（`'n'+Date.now()+亂數`），照 id 取聯集；回覆取比較長的那一串。
  刪掉的規則是**只有我撕掉的才算撕掉** —— 撕錯了再撕一次很便宜，話不見了要不回來。
  只有便利貼不一樣的話**不算衝突**（那一格已經整個合好了，沒有東西被丟掉）。
  ⚠️ 同步層把 report 交給上層時要**逐欄搬**：漏掉 `notes` 只是少一句提示，
  漏掉 `rebuilt` 是畫面不會重建 —— 骨架已經換成他的了，螢幕上還是舊的那一棵。
- **商品目錄也走同一套**（`writeCatalogue()` ＋ `mergeCatalogueInto()`）。它比檔期更該合 ——
  **全公司只有這一列**，兩個人各自貼一批商品進來，後存的把先存的整批抹掉。
  一列一個 SKU，SKU 就是那個商品的身分，所以照 SKU 取聯集；
  `model`／`brand`／`bimg`／`img` 是「商品名 → 值」的對照表，一把鑰匙一把鑰匙合。
  刪掉的規則跟便利貼一樣：**只有我刪的才算刪**（多一個商品在挑貨清單裡沒什麼，
  一整批不見要重貼一次）。
  ⚠️ 合併時**不可以用 `pullCatalogue()` 去讀對方那一份** —— 它會順手把 `catBase`
  換成剛讀到的，而那正是「對方」。祖先一旦變成對方，他新加的東西就會被當成
  「祖先本來就有、我刪掉了」而丟掉。要讀得乾淨（`readCatalogueRow()`）。
  同一個坑在 plans 那邊是 `pullSome()` 會記 stamp —— 讀取函式帶副作用，兩次都咬到。
- **誰在看哪裡**（`beat()`／`pullBeats()`）：一人一列 `__presence:<userId>`，**借 plans 那張表**
  （目前所有東西都只靠 anon key 打 REST，要 create table 就多一個未必拿得到的權限）。
  一個人只寫自己那一列 → 心跳之間永遠不會互相覆蓋。15 秒一拍，45 秒沒訊號就當作走了。
  ⚠️ plans 裡因此混進了不是檔期的列，**所有讀的路徑都要過 `isPlanRow()`**，漏一個就會在
  檔期列表上冒出一張壞掉的卡。
  ⚠️ 比對「誰在這一塊」**一定要連 planId 一起比**：版位 id 是每次開檔期從 1 開始編的（`b1`…），
  只比 blockId 的話，別人在另一檔的 b1 會在你這一檔的 b1 上亮起來。
  它擋不住任何事 —— 它做的是讓碰撞在發生**之前**被避開，那比任何合併演算法都便宜。
- 沒有存檔時預設是空的，要範例資料在網址加 `?seed=1`。「清空資料」在檔期列表右上角（行銷才看得到）。

## 寫程式的調子

- 註解用中文，寫**為什麼**，不寫這行在做什麼。既有的註解就是範例，跟著那個語氣寫。
- 位置只有四個就給四個落點讓人指，不要叫人用拖的去瞄準（刊頭換位置就是這樣改的）。
- 拖曳留給真正連續的量（高度、比例）。
- 動畫：版面重排時不要滑動 —— 列高、格數同時在變，滑動追不上，看起來像壞掉。

## git

`main` 是主線，工作開分支再發 PR。`gh` 在 `C:\Program Files\GitHub CLI\gh.exe`（可能不在 PATH 上）。
`backup/`、`turn/`、`*.bak*.html`、`out/` 都在 `.gitignore` 裡 —— 歷史本身就是備份。
