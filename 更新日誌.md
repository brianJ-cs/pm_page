# 更新日誌

## 2026-08

### 主要更新

1. **行銷併進同一個介面** —— 開檔期、切版、填品類都在這裡做完，不用再匯出檔案傳來傳去
2. **三個身分各有顏色** —— 設計天藍／PM 黃／行銷薄荷綠，一眼看得出這個視窗是誰的
3. **整體調亮** —— 桌面從深藍換成淡灰藍，不再整片壓黑
4. **預設就長得像 DM** —— 挑完貨什麼都不用搬，第一版就接近印出去的樣子
5. **少點幾下** —— 常用的東西都縮短了路徑

---

### 細項

#### ① 行銷併進來

以前行銷用的是自己那一支「落版單系統」：開檔期、切版、填品類都在那裡做完，
再匯出一份給設計和 PM 接手。現在整套併進來了 —— 三個身分從同一個網址進，看到的是同一個介面。

- 開檔表＋時間軸搬進主程式（檔期列表右上角「＋ 新增檔期」、卡片上「改時程」），
  改時程是存回同一個檔期，不會多生一筆
- 新檔期建出來是**草稿**＋一張空白的版，按「下一步：切版」直接進 總體 開始切
- 草稿只有行銷看得到，切完按右下角「**提交給設計**」才進到大家的列表裡
- 每一塊都要有品類、每一項都要有支數才按得下去，沒填完的自己框紅、鈕上直接寫還差幾個
- 切版工具（切分、移動、品類）照行銷原本那一套圖示和操作搬過來，兩邊來回的人不必記兩套
- 中間少掉的是「匯出一個檔、傳給對方、對方再匯入」這一整段 —— 提交那一下設計就看得到了

#### ② 身分顏色

一個人常常同時開兩個視窗（自己測「送給 PM」的結果），以前兩個視窗長得一模一樣。

- 三個身分沿用便利貼那套顏色：**設計＝天藍、PM＝黃、行銷＝薄荷綠**
- **最上面那條列整條是身分色**（原本只有上緣一條 3px 的細線，湊近才看得出來）
- 桌面底色混一點、名牌左邊一條，也是同一個顏色
- **紙和藍色一點都不動**：藍色永遠是「你正在點的東西」，紙是要印出去的 ——
  兩者都不該因為誰在看而變色

#### ③ 配色調亮

- 操作區的桌面從深藍（`#232a37`）換成**一張淡的灰藍**（`#ccd5e1`），只比紙深一階：
  分得出「桌面／紙」就夠了，不必整片壓黑
- 版位底色統一成**狀態**（灰＝還沒提明細、白＝在設計手上、綠＝整塊都在 PM），
  總體和小地圖從此共用同一套，不會兩邊各說各話
- 格子的底色改成**一格一個**，對應真正的 DM 上一個品類一條色帶。
  工具在 拼板 的 🎨（快捷鍵 C）：點一格上色、拉一個框整批上色、Alt 清掉；
  💧 取色（I）可以把某一格的顏色借去塗別幾格。深色底自動把字翻白。
  底色只有設計上得了，而且會跟著印出去

#### ④ 預設版面

以前挑完貨的第一版草稿還得自己搬一搬才像 DM，現在什麼都還沒動的那一版就要對。

- 格子裡的預設版面照 `rule.png` 重排：**一價／二價／三價各自成形**，
  品名、規格、商品圖、價格的落點和大小都跟著那一張走
- 價格：貼底、等距排一列、不溢出右緣和下緣，放大縮小都不會被裁掉；
  同一個版位裡的價格大小一致
- 品牌 Logo 庫併進來：**商品的品牌名對得上就自動貼圖**，不必一張一張手動指定。
  對不上的品牌在商品目錄最上面用一條琥珀色的列點名
- 切版的單位改成**公分**，尺規、游標讀數、切分長度全部同一把尺；
  版子畫出來的比例也照真實公分算（對開 78.7×54.5，以前差 2%）

#### ⑤ 少點幾下

- **切進 落版／拼板 一律先縮到整張版**，第一眼永遠是完整的版，不必每次自己按 Ctrl+0
- **左欄三個身分、三個分頁都一樣**：上面是檔期清單、下面是那一本的整本落版。
  換一檔不必退回檔期列表，在左欄點一下就換過去了
- **名牌和「登出」跟著你走**：檔期列表、編輯器工具列、開檔表標題列三條列都掛得到同一顆
- **一列旁邊的 ＋／−** 直接加減格數，不必回 總體 改支數
- **品類清單看得到、點一下就填進去**（原本是瀏覽器的 datalist，要先打對字才掉得下來，
  等於「你得先知道有什麼」才找得到它）；打完按 **Enter 就等於按那顆 ＋**
- **版上點一個空框就把對應的庫開起來**：商品圖的空框開商品目錄、品牌的空框開 Logo 庫
- **換品牌 Logo 直接把圖拖到版上那張 logo 上**，不必再開一次 Logo 庫找那家公司、
  刪掉舊的再傳一次
- **檔期卡下半可以直接拉開看小地圖**、直接改時程、直接進落版／拼板
- **整本 DM 的預覽**（總體 上那顆）把正面背面攤在一起，一眼看完整本，
  不必一塊一塊點進去；拼板進場也改成先蓋一張圖，不用看幾十格一個一個冒出來
- **別人的改動每 5 秒自己更新**，還沒動過手的話直接換成新的那一份，連提示都不跳 ——
  以前要重整才看得到

---

### 順手修掉的

- 「別人也在改」那條提示分三種情況，只有真的撞在同一塊版位時才是琥珀色警告；
  一個人用的時候不會再莫名其妙跳出來
- PM 固定三位 A／B／C，登入頁不會再自己長出 PM 丁、戊、己
- 「鎖定」拿掉了：列永遠搬得動、刊頭永遠拉得動，交件只剩「送給 PM」一個動作
- 「這一格找誰」拿掉了：三位 PM 對整本 DM 一視同仁，都看得到、點得進去、改得動
- 過往檔期和回收桶收進檔期列表右上角，刪掉是搬進回收桶不是抹掉
