# 技術 Demo HTML 撰寫原則

> 用途：主專案開發前，先把預期會遇到的難題拆解出來，分派給不同人。
> 每個人交付的成果就是**一個獨立 HTML**，證明「這個難點被解掉了」。

---

## 核心原則

### 1. 一個 HTML = 一個難點
不混雜。如果一個 demo 需要兩句話才能說明它在證明什麼，代表它裝了兩件事，該拆。

### 2. 零說明文字
不寫使用教學、不寫「這個 demo 展示了…」。
打開就能看懂、就能玩。看不懂 = demo 設計失敗，不是使用者的問題。
（例外：一行以內的操作提示可留，例如「滾輪切換圖層」。）

### 3. 用最簡方式呈現，但技術核心必須完整
「最簡」是指移除裝飾、移除業務邏輯、移除框架依賴——**不是**移除難點本身。
如果為了簡化而把最難的那 20% 拿掉，這個 demo 沒有價值。
真實的邊界條件（重疊、遮擋、非同步、失效、極端值）要留在裡面。

### 4. 對比用 switch，不要開兩個檔
同一問題有多種解法時，同一個 HTML 內用切換鈕比較。
一定要能**即時切換、同一組資料、同一個畫面**，否則對比沒有說服力。

### 5. 狀態貼在游標旁，不寫成說明區塊
現在是哪個功能、正在調整哪個對象、目前數值多少——全部用**跟隨滑鼠的浮動標籤**即時顯示。
資訊出現在游標旁邊，視線不用來回移動，也不需要側欄文字或下方說明。
**絕不做成「demo 在上、下面一長串圖文介紹」的一頁式網頁。**

---

## 補充原則

### 5. 單檔、無建置、無外部依賴
一個 `.html`，雙擊即開。不要 npm、不要 build、不要 bundler。
CSS/JS 全部內嵌。真的需要外部資源時只用 CDN，且斷網也要能看出核心行為。

### 6. 資料要假、行為要真
用寫死的假資料、picsum 圖片、mock 陣列都可以。
但**互動行為必須是真的實作**，不能是動畫演出來的假象。
Demo 是可執行的證據，不是原型圖。

### 7. 程式碼要能直接搬進主專案
Demo 的函式命名、資料結構、事件流程，就是未來主專案要用的那一份。
不要寫「反正是 demo」的爛程式。這份程式碼會被複製貼上。

### 8. 難點要能被「弄壞」
最好讓人可以手動觸發失敗情境（例如把圖層疊到完全重合、把數量拉到 1000 筆）。
能撐過壓力測試的 demo，才是真的解掉了。

### 9. 一頁看完
不捲動。核心操作區必須一眼看到。
需要捲動的 demo 通常代表塞了太多東西。

### 10. 交付即是驗收
Demo 跑起來 = 問題解決。
不需要口頭報告、不需要簡報。開檔案、動手玩、通過或不通過。

---

## 反面清單（出現以下情況，退回重做）

- 有 README 才看得懂
- 需要 `npm install`
- 核心難點被「假裝」出來（用 CSS transition 模擬計算結果）
- 一個檔案裡塞了三個不相關的功能
- 有登入頁、路由、狀態管理等與難點無關的架構
- 只有 happy path，一碰邊界就爛掉
- 程式碼是拋棄式的，不能拿去用
- **Demo 放最上面，下面接一長串文字/圖片介紹，做成一頁式網頁**
- **狀態靠旁邊的側欄文字說明，而不是貼在游標旁即時顯示**
